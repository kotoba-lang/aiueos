;; Append-only measurement series (the documented exception to "documents hold
;; current state only" -- a time series IS the value). Each entry is one run of
;; os/aiueos/scripts/os-coverage.cljs. Never rewrite an entry: a coverage number
;; that improves because someone edited history is worth nothing.
;;
;; :core is emitted/declared over :relevance :core rows. It is scoped to the
;; evidence supplied, and a zero row usually means "this profile did not
;; exercise it", not "it does not work" -- several zeros here are contradicted
;; by physical contracts (physical-kotoba-native-k16-v1.edn has
;; :arp-request-reply :passed while net-link reads 0/3, because the log that
;; proves it is not one of these logs). Raising the number therefore has two
;; honest routes and one dishonest one: feed evidence from profiles that
;; already pass, close real gaps, or widen the taxonomy denominator. The third
;; is why :declared is recorded alongside :emitted.
[{:at "2026-09-09"
  :evidence ["usb-gate-disk.out"]
  :profile :release-image-qemu-disk-boot
  :core {:emitted 29 :declared 114 :pct 25.4}
  :note "First measurement. The evidence is a single QEMU release boot, and
         that boot itself does not pass the shared UEFI suite (it stops at
         AIUEOS_VIRTIO_INPUT_FAIL, a QEMU 10.0.3 device-model difference), so
         everything after that point in the sequence is unproven here for a
         reason unrelated to AIUEOS. cluster, inference, net-link, net-stack,
         process, remote and isolation all read 0 for profile reasons rather
         than capability reasons.
         The taxonomy reported ten markers it did not know about on its first
         run and they were added; that check exists because a benchmark whose
         denominator silently stops growing flatters itself over time."}
 {:at "2026-09-09"
  :evidence ["kernel-serial.log" "uefi-debug.log" "usb-gate-disk.out"]
  :profile :uefi-suite-default-build-plus-release-image
  :core {:emitted 67 :declared 114 :pct 58.8}
  :route :a-evidence-from-a-profile-that-already-passes
  :note "25.4 to 58.8 with no capability change. The first measurement read a
         log from the USB gate, which attaches no NIC and boots the release
         image; running the shared suite directly produced 76 distinct markers
         against that log's 39. admission, memory, process and smp are 100
         percent and were never below it -- the earlier number was measuring
         the wrong boot.
         net-link and net-stack remain 0 and are still profile scope, not
         capability: no profile run so far attaches virtio-net AND reaches the
         stack. Closing them needs a network-bearing run, which is the next
         honest route (a).
         Found on the way, and larger than the number: the README claimed the
         suite fails on QEMU 10.0.3 at AIUEOS_VIRTIO_INPUT_FAIL. On exactly
         that version it emits AIUEOS_VIRTIO_INPUT_OK and finishes
         AIUEOS_UEFI_SMOKE_OK with exit 0. The gate's downgrade is the release
         IMAGE, not the host -- the build passes the suite and the release
         image does not, and the release image is what would run on a node."}
 {:at "2026-09-09"
  :evidence ["evidence-all.log" "evidence-esp-uefi.log"]
  :profile :uefi-suite-union-of-three-modes
  :core {:emitted 74 :declared 114 :pct 64.9}
  :route :a-evidence-from-profiles-that-already-pass
  :closed [:VIRTIO_NET :DHCP :IPV4 :TCP :VTD :VTD_IR :DMA_POLICY :ECDSA_PUBLIC]
  :note "58.8 to 64.9, again with no capability change, and the reason is worth
         more than the number. The suite's switches are MUTUALLY EXCLUSIVE
         MODES, not additive flags: AIUEOS_ECDSA_PUBLIC_KAT makes the guest run
         that KAT instead of the full boot. And every scenario wrote to one
         reused path, so each run overwrote the last.
         Both together meant the suite proved things no surviving file showed.
         Measured: a run with AIUEOS_TEST_NET=1 and AIUEOS_TEST_DMAR=1 exited
         0, which those switches only permit when AIUEOS_DHCP_OK, AIUEOS_VTD_OK
         and AIUEOS_DMA_POLICY_OK are all present -- the gates grep for them --
         yet the file left behind held four markers from a later, shorter mode
         and none of those three. smoke-qemu-uefi.sh now appends each run's
         serial output to evidence-all.log, after the exit status is captured
         so the copy cannot clobber it.
         net-link and net-stack were profile scope exactly as suspected, and
         are no longer zero.
         CAVEAT ON THE 100 PERCENT: isolation reads 6/6 on VT-d markers
         measured under QEMU emulating Intel. The K16 is an AMD Ryzen 7 7735HS.
         That row's coverage transfers to this board not at all, and it is the
         one place where a rising number here means less rather than more.
         Next lever, untried: AIUEOS_SSH_HOSTFWD against remote 0/6."}
 {:at "2026-09-09"
  :evidence ["evidence-all.log" "evidence-esp-uefi.log"]
  :profile :uefi-suite-union-plus-amd-vi-observation
  :core {:emitted 74 :declared 114 :pct 64.9}
  :route :b-a-defect-in-the-measurement-itself
  :note "The headline did not move and the measurement got better, which is the
         only combination this ledger should ever be pleased about.
         Booting the guest behind AMD-Vi for the first time -- the K16's actual
         IOMMU class, which QEMU 10.0.3 can emulate -- showed
         AIUEOS_DMA_POLICY_OK emitted as `dmar=absent test-only-unisolated`.
         Under VT-d the same marker reads `dmar=validated dma=vtd-isolated`.
         One name, two opposite meanings, and this script counted names: it was
         crediting an explicitly unisolated boot with isolation.
         Fixed by letting the taxonomy require a payload substring for markers
         whose _OK is a report rather than a verdict. The first substring chosen
         was `isolated` and it discriminated NOTHING, because
         `test-only-unisolated` contains it -- the negative case is spelled as
         the positive with a prefix. Caught by running the control rather than
         trusting the string; `validated` separates them.
         Verified both directions: AMD-Vi-only evidence now reads isolation 3/6
         with DMA_POLICY unproven, and evidence containing a real VT-d boot
         still reads 6/6. The union stays 74/114 because it genuinely contains
         an isolated boot."}
 {:at "2026-09-09"
  :evidence ["evidence-all.log" "evidence-esp-uefi.log"]
  :profile :uefi-suite-union
  :core {:emitted 74 :declared 115 :pct 64.3}
  :route :b-a-gap-closed-halfway-and-the-denominator-grew
  :note "64.9 to 64.3, and down is right. acpi.c now discovers and validates
         IVRS -- AMD's counterpart to DMAR, and the table that actually exists
         on this board -- with the same fail-closed shape as DMAR: a table that
         claims to be IVRS and does not validate stops the boot rather than
         being skipped, because skipping leaves the present flag at 0, which is
         indistinguishable from a machine with no IOMMU, and that is exactly
         the state the DMA policy reads as permission to run unisolated.
         The guest now reports three distinguishable states instead of one:
         IVRS_OK validated, IVRS_FAIL described-but-refused, IVRS_ABSENT
         no-amd-iommu-described.
         AIUEOS_IVRS joins the isolation row, so :declared grows to 115 and the
         headline falls. That is the honest direction and the reason :declared
         is recorded beside :emitted: the tree grew a capability it has not
         proven.
         And a correction to this session's own claim. I wrote that
         amd-ivrs-isolation was NOT hardware-gated because QEMU 10.0.3 offers
         amd-iommu. QEMU has the DEVICE and does not hand the guest an IVRS
         TABLE: attached, with and without intremap=on, the guest reports
         IVRS_ABSENT. The parser is correct by construction and untested
         against a real table. Marked as such rather than counted."}
 {:at "2026-09-09"
  :evidence ["evidence-all.log" "evidence-esp-suite.log"]
  :profile :uefi-suite-union-narrower-evidence
  :core {:emitted 71 :declared 115 :pct 61.7}
  :note "64.3 to 61.7, and this one is a measurement artifact rather than a
         change in the system: the second log is a single fresh ESP run, where
         the earlier reading paired the union with a wider capture. Recorded
         rather than smoothed, because a series that only moves when the news
         is good is not a series.
         The node advanced instead. release-image-enumerates-devices is
         renamed release-image-boots-to-completion, because measuring it
         changed what it is. pci_result is now printed as
         AIUEOS_PCI_RESULT -- deliberately not an _OK, since it is a datum and
         this benchmark counts verdicts -- and with it visible, the GPT boot
         turned out to emit no PCI line at all in that run, neither PCI_OK nor
         PCI_FAIL, ending at APIC_TIMER. It is not enumerating differently. It
         is stopping earlier, at a point that moves between runs.
         The ESP profile is unregressed at 76 markers with
         input-rng-base=111, checked because two of these edits touched the
         boot path and a regression would otherwise have been read as a
         finding."}
 {:at "2026-09-09"
  :evidence ["evidence-all.log" "evidence-esp-suite.log"]
  :profile :uefi-suite-union
  :core {:emitted 74 :declared 115 :pct 64.3}
  :note "The number did not move and the diagnosis did, twice.
         Every 0x6f exit now names its own line -- it is reached from
         twenty-seven places, so the code named a class and not a failure, the
         same position exit 63 was in. The release image stops at main.c:3058,
         the branch for pci_result & 4 clear.
         Then counting devices seen settled what that meant. Both profiles
         report input-devices-seen=1: the virtio-input device is PRESENT in the
         release image's boot, and virtio_input() fails to bring it up. Not an
         enumeration difference, not an early stop -- device initialisation.
         That is the third reading of this node in three iterations, and the
         pattern is worth keeping: absent markers, then a missing PCI line,
         then a result bit, each consistent with what was visible and wrong
         about what was happening. The measurement that settled it was the
         cheapest of the three and could have been taken first."}
 {:at "2026-09-09"
  :evidence ["evidence-release-image.log"]
  :profile :release-gpt-image-built-as-its-own-gate-requires
  :core {:emitted 66 :declared 115 :pct 57.4}
  :note "25.4 to 57.4 for the release image, and nothing about the image
         changed -- only how it was built.
         virtio_input fails at its last exit, after
         #ifdef AIUEOS_INPUT_SMOKE_SYNTHETIC: no event arrived and no synthetic
         one was compiled in. smoke-qemu-uefi.sh builds its own kernel with
         AIUEOS_INPUT_SMOKE_SYNTHETIC=1 and build-release-image.sh does not, so
         every measurement of the release image so far was of an artifact built
         without a define its own gate requires. Built with it: 79 markers,
         more than the ESP build's 76.
         So the release image was never half broken, and this ledger said it
         was, three entries running. The finding that survives is smaller and
         sharper: the suite cannot pass a genuine production image by
         construction, because a shipping image must not fabricate input events
         and the suite requires one. A gate design problem, not an OS defect.
         Also worth keeping: the number was read wrong once more before it was
         read right. input-fail-line printed 128, which is not a line in the
         function -- my own grep window truncated 1284. The instrument, not the
         system."}
 {:at "2026-09-09"
  :evidence ["short-int.txt" "kernel.map"]
  :profile :release-gpt-image-hang-localised
  :note "No coverage change; a diagnosis. The release image's long-running
         nondeterministic stop is a HANG -- exit 124, and not one exception in
         the entire interrupt log, only timer and MSI-X.
         build-uefi.sh now emits a link map beside the stripped kernel, which
         is what made the address readable. Every numeric diagnosis in this
         tree has needed one and none had it: exit 63 took days, 0x6f named
         twenty-seven call sites, and this hang could be seen but not named.
         The map costs nothing at runtime and ships nowhere.
         With it: the BSP spins in virtio_blk_sector_io at 0x1163c2 while
         vector 0x23 -- 35, the virtio-blk MSI-X vector by the evidence's own
         account -- fires repeatedly. The completion arrives and the loop never
         finishes. A storage-path hang.
         Two readings were discarded first, both the same error. The log's last
         events are in task_loop, which is the IDLE AP rather than the stuck
         BSP; the two stack pointers differing by exactly 0x1000 were two CPUs.
         And -smp 1 does not isolate the BSP -- this guest requires cpu>=2 and
         fails ACPI honestly, exit 241. Filtering events by address against the
         map is what separated the processors."}
 {:at "2026-09-09"
  :profile :release-gpt-image-hang-no-longer-reproduces
  :note "No coverage change. The wait that hangs tests
         `used->index == target` -- exact equality on a counter that only
         advances -- while the IRQ half of the same condition uses >=. If the
         used index ever passes target the loop can never succeed, and it keeps
         taking the device's MSI-X for a completion it has already missed. That
         is a hang rather than a delay, and the two want different treatment.
         virtio_blk_sector_io now detects exactly that condition -- used index
         past target, which no future completion can undo -- and returns with
         the numbers instead of spinning until the harness gives up. It is a
         LOGICAL bound, not a timing one: no threshold to tune and nothing that
         fires on a slow but healthy device.
         Measured after: the ESP profile is unaffected, VIRTIO_BLK_OK and exit
         0. The release image stopped hanging -- seven runs, all reaching the
         same deterministic stop, the already-explained input one.
         STATED CAREFULLY: the overshoot detector did NOT fire. So the cause is
         not confirmed. Either the check catches nothing and its presence
         changed timing enough to hide the race, or the hang has another source
         that seven runs did not sample. What is established is that the boot
         became deterministic where it was not, and what is not established is
         why."}
 {:at "2026-09-09"
  :profile :blk-wait-measured-on-the-healthy-path
  :note "A negative result, and it is against my own hypothesis.
         The overshoot theory needed the used index to have room to run ahead
         of target. Measuring the SUCCESS path says it does not: on both
         profiles the slowest wait that ever succeeded took ONE iteration, with
         used equal to target -- 49 requests on the build, 55 on the release
         image. A completion that is seen on the first look cannot arrive twice
         before anyone checks.
         Combined with the overshoot detector never firing across nine runs, of
         which zero hung, the honest position is: the hang no longer reproduces
         and nothing here explains why it did. The obvious candidate -- that
         adding the check fixed it -- is not supported: it never triggered. The
         likelier story is that some other change between those builds moved the
         timing, since several landed in between, and that is a story rather
         than a measurement.
         The check stays on its own merits. An exact-equality wait on a
         monotone counter beside an inequality on the counter next to it is
         fragile whether or not it is what bit us, and now it fails with
         numbers instead of spinning. But the node is NOT closed and the cause
         is NOT known, and the difference between those and a green run is the
         whole point of keeping this ledger."}
 {:at "2026-09-09"
  :profile :reproducible-build-node-closed
  :note "A node closed by discovering it never existed.
         It said three builds of an unchanged tree gave three KERNEL.ELF
         hashes because build identity is embedded. The tree was NOT unchanged
         -- I had been editing C sources between those builds -- and the
         identity header holds version, commit[:12] and a dirty flag, with no
         timestamp anywhere in it, so it could not have produced the difference
         I blamed it for. I read three hashes and inferred a mechanism instead
         of testing one.
         Tested: two builds back to back with nothing touched give a
         byte-identical KERNEL.ELF, 6eb33f67..., and building into a different
         output directory gives the same bytes again. Reproducible here, and
         path-independent.
         NOT claimed: across machines, toolchain versions or clocks. Never
         measured, so the node asserts nothing about them.
         This is the fourth claim of mine to be retracted in this branch, and
         all four have the same shape -- a mechanism inferred from an
         observation that had a simpler explanation. The three before it were
         about the release image."}
 {:at "2026-09-09"
  :profile :tpm-node-advanced-then-blocked
  :note "No coverage change. tpm-key-binding verified before it was quoted:
         zero files in the tree mention tpm, tcg2 or measured boot, so the
         contract's claim is true and the work is from scratch.
         Landed the same first step that worked for IVRS -- TPM2 ACPI table
         discovery, fail-closed, three reported states -- and installed swtpm
         0.10.2 so the emulator half exists.
         Then measured before building further, which is the whole lesson of
         the IVRS iteration: with swtpm running and tpm-tis attached, QEMU and
         swtpm report no error and the guest still says TPM2_ABSENT. An
         emulated TPM does not reach this guest with the OVMF available here.
         The cause is not established, and the obvious test was invalid: I
         checked the firmware volume for TPM strings and got zero, then ran the
         control and DMAR also returned zero -- while demonstrably reaching the
         guest, because the volume is compressed. The control disproved my test
         method rather than the hypothesis. Reported as unknown.
         ESP suite unaffected: 76 markers, exit 0."}
 {:at "2026-09-09"
  :profile :loop-reached-its-stop-condition
  :note "Zero nodes remain that an agent can reach. Recorded here because the
         loop's own stop condition is a claim like any other and should be
         checkable: the reachability is computed from the graph, not judged.
         The last attempt was tender-reentry-proven, and it is worth keeping
         for how it failed. A fixture guest was written rather than making the
         real guest fake a 250 -- production code faking a value to satisfy a
         test is how a gate stops meaning anything. It does not build, twice
         over and both measured: a minimal guest cannot pass the RX/RW
         adjacency check, because the emitter puts RW at a fixed 0x108000 and
         anything under about 28 KB of text leaves a gap; and wrapping the real
         kernel instead fails project-link because that module is not on the
         fixture build's explicit source paths.
         Both are lifted by editing a production build script for a test's
         benefit, or by adding a bypass to a safety check. Neither is mine to
         decide, so the loop stops rather than talks itself into one.
         Final: 16 nodes, 1 done, 15 behind six gates. Coverage 65.2 percent,
         from 25.4 at the start. Five claims retracted, two of them mine."}
 {:at "2026-09-09"
  :evidence ["evidence-all.log" "evidence-esp-suite.log" "evidence-release-image.log"]
  :profile :union-plus-ssh-gates
  :core {:emitted 78 :declared 116 :pct 67.2}
  :route :a-three-gates-that-could-not-run-now-run
  :closed [:SSH_LISTEN :SSH_KEX]
  :note "65.2 to 67.2, with the denominator also growing by one as TPM2 joined
         the crypto row after the drift check reported it. Recorded after the
         stop entry above rather than before it: the loop stopped because no
         PLAN node was reachable, and coverage work is not a plan node.
         Three SSH gates were neither green nor red -- smoke-qemu-ssh-kex,
         -auth and -session all died on `Could not find namespace:
         ssh.transport` while org-ietf-ssh sits checked out. Same shape as the
         kotoba.lang.text breakage this repo already fixed, and fixed the same
         way, with the worktree caveat written down beside it: those sibling
         paths resolve relative to nbb.edn, so they work in the west checkout
         and not under .claude/worktrees, where .. is the worktree directory.
         With them running they discriminate. ssh and ssh-kex pass; auth and
         session fail for a REAL reason -- AIUEOS_SSH_KEX_REPLY_INCOMPLETE
         stage=2, then AIUEOS_SSH_LISTEN_INCOMPLETE stage=0. remote stops at
         LISTEN and KEX because the kernel's SSH is incomplete past there,
         which is a gap to close rather than a lever to pull.
         A watchdog comparison with an empty operand was guarded; it printed
         `<timestamp>: integer expression expected` once per tick and buried
         the gates' verdicts. NOT fully fixed -- the noise fell from hundreds
         of lines to twenty, so at least one more unguarded comparison
         remains."}
 {:at "2026-09-09"
  :evidence ["evidence-all.log" "evidence-esp-suite.log" "evidence-release-image.log"]
  :profile :union-plus-receipts
  :core {:emitted 83 :declared 116 :pct 71.6}
  :route :a-triggers-paired-with-their-expectations
  :closed [:FAULT_RECEIPT :CRASH_RECEIPT :PANIC_RECEIPT :ECDSA_SIGN]
  :note "67.2 to 71.6. interrupts reaches 9/9, crypto 8/9, storage 16/17, and
         six rows are now complete: admission, interrupts, memory, process,
         services, smp.
         The receipts took two tries because the first was the wrong shape.
         AIUEOS_EXPECT_FAULT is an EXPECTATION, not a trigger -- running it
         alone asserts a fault that nothing injected. The trigger is a build
         define, AIUEOS_FAULT_RECEIPT_SMOKE, and the two only work as a pair.
         smoke-qemu-journal-recovery.sh already pairs the crash ones, and
         reading an existing gate was faster than deducing the pairing.
         That is the same distinction the release image turned on: a run flag
         and a build define are different things, and a gate that needs both
         fails silently when given one. Third time this pattern has cost a
         wrong reading in this branch.
         What remains unproven is now mostly not a lever: cluster and inference
         need the owner, PHYSICAL_* and IVRS and TPM2 need the board or a
         firmware, and remote needs the kernel's SSH finished past KEX."}
 {:at "2026-09-09"
  :evidence ["evidence-all.log" "evidence-esp-suite.log" "evidence-release-image.log"]
  :profile :union-with-both-transports
  :core {:emitted 86 :declared 116 :pct 74.1}
  :route :a-half-the-evidence-was-never-collected
  :closed [:KERNEL :LOADER_INTEGRITY]
  :note "71.6 to 74.1, and the capability did not change: the accumulator was
         collecting half of what the guest says.
         The guest writes to TWO transports and they do not carry the same
         markers. AIUEOS_KERNEL_OK and AIUEOS_LOADER_INTEGRITY_OK are loader
         stage and go to debugcon, never to serial, so an accumulator that took
         only kernel-serial.log reported them unproven while every boot emitted
         them. smoke-qemu-uefi.sh now appends both.
         Noticed by asking why a marker the release image demonstrably printed
         was reading as unproven -- the same question that found the overwriting
         problem. Both times the measurement was wrong and the system was fine.
         boot stops at 6/9. The three that remain are not levers:
         LOADER_MODEL_OK needs a real GGUF handoff, NETBOOT_EMBEDDED_OK the
         PXE-embedded image path, and LOADER_RECOVERY_OK a corruption that
         actually forces the fallback -- the three AIUEOS_CORRUPT_* modes all
         pass without reaching it."}
 {:at "2026-09-09"
  :profile :unproven-markers-classified
  :core {:emitted 86 :declared 116 :pct 74.1}
  :note "No number moved; the report learned to answer a question it could not.
         ACPI_RETAIN turned out already proven once both transports were
         collected, so platform is 7/8 and thirty core markers remain unproven.
         A single count treated them alike when they need five different
         things, which is the flattening this file's own header warns about --
         a zero meaning 'this profile did not exercise it' read as a zero
         meaning 'it does not work'.
         The report now says what each waits on:
           owner 9, board 8, model-artifact 7, unwritten 3, profile 2,
           firmware 1.
         The useful line is the third from the end. THREE markers are unwritten
         code -- SSH_KEX_REPLY, SSH_AUTH, SSH_SESSION -- and they are the only
         group an agent can close without a credential, a powered board, a
         10.9 GB artifact or a different firmware. The auth gate already names
         the stage: AIUEOS_SSH_KEX_REPLY_INCOMPLETE stage=2.
         A marker absent from the map counts as :unclassified and says so,
         because 'not attributed' is not 'unblocked' and the difference is
         exactly what this benchmark keeps getting caught on."}
 {:at "2026-09-09"
  :profile :ssh-group-reclassified
  :core {:emitted 86 :declared 116 :pct 74.1}
  :note "Correcting the classification landed one iteration ago, and the
         correction is the entry.
         SSH_KEX_REPLY, SSH_AUTH and SSH_SESSION were labelled :unwritten. The
         code is all there -- pci.c drives ssh_kex_stage 1 through 16 and
         main.c reports at 5, 12 and 16. What fails is the LIVE exchange, and
         it fails differently between runs: one reached stage 2, the client's
         KEXINIT received and KEX_ECDH_INIT never arriving, and the next
         produced no client banner at all.
         So the actionable conclusion survives -- this is still the only group
         an agent can close without a credential, a board, an artifact or a
         firmware -- but 'write the missing code' was wrong and would have sent
         the next reader hunting for code that already exists. Relabelled
         :live-exchange-stalls.
         Found by opening the file the label pointed at instead of trusting the
         label, one iteration after writing it. That is the same check this
         branch applies to other people's contracts, applied to my own, and it
         is the sixth claim here to need retracting."}
 {:at "2026-09-09"
  :profile :ssh-session-evidence-preserved
  :core {:emitted 89 :declared 116 :pct 76.7}
  :note "SSH_SESSION closed, and the way it closed is the entry.
         The gate printed a bare AIUEOS_SSH_SESSION_OK with no payload, while
         the guest's line reads 'session channel exec channel-data streamed'.
         I took that as a sign the gate might be synthesising its own verdict,
         which would make feeding it to the coverage script a way of counting
         the gate's opinion as proof. Reading the script reversed it: line 229
         does str/includes? on the guest's serial log, and the branch that
         prints the guest's raw line is guarded by when-not kernel-ok -- so the
         payload is withheld exactly on SUCCESS. The bare label was faithful.
         The real defect was next to it. The gate concludes early on purpose
         (waiting for qemu_exit past the timeout triggers a reboot that
         overwrites the serial) and kills the process group, so
         smoke-qemu-uefi.sh never reaches its own append. AUTH_OK and
         KEX_REPLY_OK were in evidence-all.log four times each; SESSION_OK was
         there zero times. The proof existed only as gate stdout, which no
         evidence log ever sees. Same one-transport family as the overwrite bug
         fixed earlier on this branch, in the one gate that concludes early.
         conclude now appends the serial before kill-group. Measured rather
         than assumed: evidence-all.log 348459 -> 356762 bytes, SESSION_OK
         0 -> 1, and the line that arrived carries the full payload.
         remote 4/6 -> 5/6. What remains there is RTL8125_SSH_SESSION, which
         is board-gated, so the group an agent can close without a credential,
         a board, an artifact or a firmware is now empty.
         Also cleared a stale QEMU from the previous session's early conclude:
         it still held hostfwd 8022 and had the deleted serial open, which is
         why the run before this one died on 'Could not set up host forwarding
         rule'. kill-group kills the wrapper's group; that QEMU outlived it."}
 {:at "2026-09-10"
  :profile :release-image-evidence-collected
  :core {:emitted 90 :declared 116 :pct 77.6}
  :note "LOADER_RECOVERY was case (a) -- a profile that already passes whose
         evidence was never collected -- and confirming which case it was is
         most of the work. uefi/main.c:1081 emits it, its gate
         smoke-qemu-release-image.sh already checks BOTH directions (requires
         the marker at line 99, requires its absence in the control at 160),
         the gate needs no board, and it exits 0. So nothing was broken; the
         log simply was not being measured. Added relout/evidence-all.log to
         the measurement. boot 6/9 -> 7/9, core 89 -> 90 of 116.
         The side finding is worth more than the marker. That gate printed
         '[: integer expression expected' on every watchdog tick -- 80 times in
         one run -- while exiting 0. GNU coreutils stat is first on PATH here;
         it reads -f as FILESYSTEM status, says %m is not a filesystem format,
         prints the blob for the file anyway, and EXITS 0. So
         'stat -f %m || stat -c %Y || echo 0' never reaches either fallback and
         watchdog_t silently becomes that blob. A probe that fails while
         reporting success, whose own guard is unreachable because of the
         success. The comment above the line already described the symptom
         (the watchdog stops advancing, so a quiet guest is measured from the
         wrong instant); what nobody had noticed is that the fallback written
         to prevent it could not run.
         Fixed by validating the FORMAT (case ...*[!0-9]*) instead of the exit
         code. Measured both directions standalone -- real file 1786801082,
         missing file 0 -- and then in situ: same script, same out dir,
         80 errors -> 0, AIUEOS_UEFI_SMOKE_OK, exit 0.
         Remaining profile-gated: NETBOOT_EMBEDDED alone. Its two gates
         (smoke-qemu-physical-{persistent,pxe}.sh) need only timeout and
         qemu-system-x86_64 -- 'physical' names the boot path, not the
         hardware -- but both read AIUEOS_ALLOW_DIRTY_QUALIFICATION_BUILD and
         this worktree is dirty, so they may refuse rather than run. A refusal
         is a third outcome, neither pass nor gap, and must not be recorded as
         either."}
 {:at "2026-09-10"
  :profile :netboot-evidence-preserved-board-guard
  :core {:emitted 91 :declared 116 :pct 78.4}
  :note "NETBOOT_EMBEDDED was case (a): smoke-qemu-physical-persistent needs no
         board (timeout + qemu only -- 'physical' names the boot path), it
         exits 0, and it then DELETED the only record of it, because the debug
         log lives in a mktemp dir under `trap rm -rf EXIT`. A gate that proves
         a marker on every green run and destroys the proof is indistinguishable
         from one that never ran. It now appends debug+serial to an evidence log
         before the trap fires. boot 7/9 -> 8/9, core 90 -> 91 of 116.
         Preserving it alone would have been wrong, and this is the part worth
         keeping. The same log carries AIUEOS_PHYSICAL_QUALIFICATION_OK, which
         the taxonomy classes :board; kernel/main.c:2816 prints it on any host
         that reaches the native core, so os-coverage would have counted it the
         moment the log was added. The gate answers this itself -- it ends with
         physical-k16=unverified -- and the claim the marker carries,
         internal-disk-writes=none, is vacuous where no internal disk exists.
         Counting it would prove from a QEMU run exactly what that run says it
         has not proven. Not (c) by widening the denominator, but (c) by
         loosening what counts as proof, which is the same thing one level down.
         So the provenance line is written INTO the evidence and os-coverage
         drops :board markers from any log declaring physical-k16=unverified.
         That is the existing payload-ok? reasoning ('the name alone credited an
         explicitly unisolated boot with isolation') moved from the line to the
         log.
         The guard was measured in both directions, on the same bytes: with the
         provenance line 91/116 78.4% and platform 7/8; with only that line
         removed 92/116 79.3% and platform 8/8. So the guard is worth exactly
         one marker and the temptation it removes was real -- without it this
         entry would have claimed 79.3%. Regression: on the two logs that carry
         no such line the report is unchanged at 90/116.
         Also recorded: the gate REFUSED a run mid-iteration --
         'qualification release must be built from a clean tracked tree' --
         because my own uncommitted edits had dirtied it. That is a third
         outcome, neither pass nor gap, and the fix was to commit and satisfy
         the rule rather than set AIUEOS_ALLOW_DIRTY_QUALIFICATION_BUILD=1,
         which would have made the gate answer about a tree that is not the one
         being qualified.
         Remaining core unproven 25: owner 9, board 8, model-artifact 7,
         firmware 1. The profile-gated group is now empty."}
 {:at "2026-09-10"
  :profile :tpm2-measured-absent-no-marker-closed
  :core {:emitted 91 :declared 116 :pct 78.4}
  :note "Nothing closed. 91/116 is unchanged and that is the result, not a
         pause before one.
         Two reasons in the taxonomy were beliefs; both are now measurements.
         TPM2 said 'needs a firmware this machine does not have (TPM-enabled
         OVMF)'. That is two claims and neither had been tested. swtpm and
         swtpm_setup ARE installed here and QEMU has the emulator backend, so
         the device side was available all along -- the reason as written would
         have let a reader conclude the whole thing was unavailable. Added an
         AIUEOS_TEST_TPM2 hook mirroring the existing IVRS one and ran it: with
         a real software TPM attached the guest reports
         AIUEOS_TPM2_ABSENT no-root-of-trust-described, twice. So the blocker is
         OVMF declining to publish a TPM2 ACPI table, not the absence of a TPM.
         The reason was right for a reason nobody had checked. Hook kept, and
         the re-test command is now written next to the classification.
         model-artifact (7) is correctly classified -- NOT a mislabelled
         :profile like the two before it. But its reason was incomplete:
         Qwen3.8-27B-UD-IQ3_XXS.gguf (10,934,860,704 B) is absent AND the volume
         has 16 GB free of 927 GB at 99% full, which the release-image builds
         also write into. 'Needs the artifact on hand' invites 'one download
         away'; on this machine the download would take 68% of the remaining
         headroom. Recorded, not fetched -- that is a decision about the owner's
         disk, not a coverage decision.
         I also broke something and caught it only because two outputs
         disagreed. The TPM block used `trap ... EXIT HUP INT TERM`, which
         REPLACES rather than adds, silently removing the script's existing
         cleanup_plc_signing traps. The run printed AIUEOS_UEFI_SMOKE_OK and
         exited 1. Had the marker verdict been all I looked at, I would have
         reported TPM2_ABSENT and left a disabled cleanup behind. Folded into a
         single cleanup_all with one pair of traps and a comment at the trap
         site saying why a second EXIT trap below would break it. Discriminated:
         same TPM2 verdict, exit 1 -> exit 0.
         Remaining 25 -- owner 9, board 8, model-artifact 7, firmware 1 -- and
         none is agent-closable on this machine without a credential, a board,
         11 GB of disk, or a different OVMF."}
 {:at "2026-09-10"
  :profile :plc-rt-classified-compiler-gap
  :core {:emitted 91 :declared 116 :pct 78.4}
  :note "First: I was wrong last iteration to say the loop had reached its stop
         condition. I had read only the CORE rows. The report also carries two
         rows measured but excluded from the headline -- display 13/16 and
         realtime 0/1 -- and all four of their markers (PLC_RT, GOP_DISCOVERY,
         GUEST_INPUT, GUEST_SCANOUT_TWO) are UNCLASSIFIED in :unproven-because.
         The contract says in as many words that unclassified is not unblocked.
         Excluded from the headline is exactly where an unexamined marker can
         sit forever, and I nearly closed the loop on top of four of them.
         PLC_RT is now classified :compiler, and getting there took four
         measurements that each overturned the previous conclusion:
         (1) build-plc-native-program.sh against the shared amu checkout ->
             exit 70 :kotoba/internal-error. Not a compiler bug: that checkout
             (72e67e5e) is DIVERGED from the west pin (fac4176c), neither an
             ancestor of the other, pin +27. README asks for the pinned one.
         (2) A detached worktree at the pin -> MODULE_NOT_FOUND nbb/cli.js.
             Still not about the compiler; a fresh worktree is not installed.
             Did not npm install onto a 99%-full disk. Checked instead whether
             the existing install is VALID for the pin: package-lock.json and
             nbb.edn identical, and the three dependency maps hash identically;
             the only package.json difference is one npm script line. Symlinked.
         (3) Pinned amu, deps present -> exit 70, same :kotoba/internal-error.
             NOW it is evidence, and it reproduces on the pinned toolchain.
         (4) Bisected, because 'the compiler crashes' is not actionable:
             (defn main [] 0) compiles to 8560 bytes, plc-dint and its 2^32
             arithmetic compile, and (defn main [] (cap-call 16 0)) crashes.
             plc-runtime-policy.edn allows [:cap/call 16..19] explicitly, so a
             PERMITTED form is crashing the backend -- not a refusal wearing a
             bad message.
         So PLC_RT is (b), a real gap, and it is in amu rather than aiueos.
         Recorded with the three-line repro and the pin SHA next to the
         classification. Nothing closed: 91/116 78.4% unchanged, realtime still
         0/1. A classification is not a marker.
         Second, smaller gap found on the way: build-plc-native-program.sh
         writes build/aiueos-plc/<name>/program.elf while smoke-qemu-uefi.sh
         defaults AIUEOS_PLC_ELF to build/plc-motor/program.elf. Even a fixed
         compiler leaves this documented profile not running unless someone
         passes the path by hand.
         Also worth its own attention, outside this loop: the shared amu
         checkout is diverged from its west pin. The toolchain-sync rule exists
         because that once made a real capability look absent for days. Not
         touched -- it holds uncommitted work."}
 {:at "2026-09-10"
  :profile :display-row-two-closed-one-classified
  :core {:emitted 91 :declared 116 :pct 78.4}
  :display {:emitted 15 :declared 16 :pct 93.8}
  :note "The display row, which last iteration's correction opened up. Its three
         unclassified markers turned out to be three different things, and the
         only way to tell was to measure each.
         GUEST_INPUT: (a). AIUEOS_GUEST_INPUT=1 is a run flag that builds
         WITHOUT AIUEOS_INPUT_SMOKE_SYNTHETIC (hence the required synthetic=0),
         attaches an id'd keyboard plus a QMP socket, and injects real key events
         from python. Complete profile, simply never run. Landed
         AIUEOS_GUEST_INPUT_OK eventq-used=1 synthetic=0.
         GOP_DISCOVERY: (a), and subtler. It prints only under
         if (gop_used_protocol_scan), so it marks the FALLBACK discovery path.
         Under OVMF the ConsoleOut handle carries the GOP, the fast path always
         wins, and the marker can never appear no matter how many times the
         normal gate is green. The build define that forces the fallback,
         AIUEOS_GOP_FORCE_PROTOCOL_SCAN, already existed and was already wired
         into build-uefi.sh. Landed AIUEOS_GOP_DISCOVERY_OK source=protocol-scan.
         display 13/16 -> 15/16 93.8%. CORE unchanged at 91/116 78.4%, because
         display is an excluded row -- these two do not move the headline and
         should not be read as if they did.
         GUEST_SCANOUT_TWO: (b), a real gap, classified :code. No new boot was
         needed -- the kernel prints its own failure branch and the logs already
         on hand said it: 'leftover=one-scanout enabled=1', 36x in one and 7x in
         another, while AIUEOS_GUEST_GPU_TWO_OK resources=2 flush=2 passes in the
         SAME boots and QEMU runs with max_outputs=2. Two GPU resources bind and
         one scanout enables. The number to move is aiueos_gpu_enabled_scanouts()
         -> 2. Evidence for a gap was sitting in logs measured four iterations
         ago; nothing had ever read the failure branch.
         Also fixed a fragility that cost a run. The gate derives its QMP socket
         path from $out and a UNIX socket path is capped at 104 bytes. My
         scratchpad out gave 135 and QEMU died with a message naming neither the
         profile nor the cause. The DEFAULT out measures 99 -- five bytes of
         headroom -- so a checkout directory a few characters longer silently
         makes this documented profile unrunnable. It now refuses early and says
         which path, how long, and what the limit is. Discriminated both ways:
         135-byte path -> exit 1 with that message; short path -> exit 0,
         refusal silent, AIUEOS_UEFI_SMOKE_OK.
         k16 smoke green, but honestly: attempt 1 gave PSTCMPRCD without X and
         it passed on attempt 2. That is the known teardown race, which the gate
         absorbs by requiring X in one of four attempts. Green by contract, not
         green in one shot.
         Remaining: CORE 25 (owner 9, board 8, model-artifact 7, firmware 1),
         plus GUEST_SCANOUT_TWO :code and PLC_RT :compiler in the excluded rows.
         Verified rather than asserted, and the scope matters: the report's
         reason tally sums to exactly 25 with no unclassified bucket, which
         covers the CORE rows only. The two excluded-row markers were
         classified by hand this iteration, so nothing in either group is
         unclassified now -- but only the core half of that is machine-checked,
         and the report has no equivalent tally for the excluded rows."}
 {:at "2026-09-10"
  :profile :scanout-two-closed-my-classification-was-wrong
  :core {:emitted 91 :declared 116 :pct 78.4}
  :display {:emitted 16 :declared 16 :pct 100.0}
  :note "GUEST_SCANOUT_TWO closed, and the entry is the retraction.
         Yesterday I classified it :code -- 'a real gap in aiueos itself' -- on
         the strength of the kernel's own failure branch: leftover=one-scanout
         enabled=1, 36x in one log and 7x in another, while GUEST_GPU_TWO_OK
         resources=2 flush=2 passed in the same boots under max_outputs=2. The
         evidence was real. The inference was not checked. I never asked whether
         a profile existed that changes the INPUT to that branch.
         One does, and the script documents it fifteen lines above the QEMU
         invocation. QEMU sets enabled_output_bitmask=1 at realize and a head
         becomes enabled only when a UI frontend calls ui_info with a non-zero
         size. `-display none` never does, so under the default backend
         GET_DISPLAY_INFO CANNOT report two scanouts however correct the guest
         is -- and the guest refusing to drive a head the host calls disabled is
         correct behaviour, not a gap. AIUEOS_GUEST_SCANOUT_TWO=1 selects the
         dbus backend and enables head 1 with gdbus SetUIInfo on Console_1.
         Only dbus-daemon was missing; installed it (the gate's own error
         message names brew install dbus). Ran it: AIUEOS_GUEST_SCANOUT_TWO_OK
         scanouts=2 resource-0=1 resource-1=2 kotoba-n=2, exit 0.
         display 15/16 -> 16/16 100%. CORE unchanged at 91/116 78.4%; display is
         an excluded row and closing it does not move the headline.
         This is the same shape as GOP_DISCOVERY one iteration earlier -- a
         marker a passing gate structurally cannot produce -- and I had written
         that lesson down in the previous report before making its mirror image
         here. Reading a failure branch as a gap is easy precisely because the
         branch is honest about what happened; it says nothing about why, and
         the why was one grep away. Corrected in place to :profile with the
         mechanism, rather than swapped quietly.
         Every display marker now passes. Remaining: CORE 25 (owner 9, board 8,
         model-artifact 7, firmware 1) and PLC_RT :compiler. Nothing left that an
         agent can close on this machine: PLC_RT needs amu to stop crashing on a
         cap-call its own policy permits."}
 {:at "2026-09-10"
  :profile :plc-rt-confirmed-blocked-upstream
  :core {:emitted 91 :declared 116 :pct 78.4}
  :note "Nothing closed. The iteration eliminated a hypothesis instead, which is
         worth an entry because it stops the next reader repeating it.
         Last time I wrote that the agent-closable set was empty. Having been
         wrong about exactly that twice this session, I checked one avenue I had
         never examined: I had tested the cap-call crash against the shared amu
         checkout and the west pin, both DIVERGED from amu's real origin/main. If
         the crash were fixed upstream, PLC_RT would be reachable by advancing
         the pin -- a sanctioned operation.
         Fetched and measured. origin/main is 2abd9843, the pin IS its ancestor,
         +2 commits, so a pin advance would be a clean fast-forward. Built a
         detached worktree there (dependency manifests identical to the installed
         tree, so the existing node_modules is valid) and ran the same three-line
         repro: exit 70, :kotoba/internal-error, identical. So the crash holds at
         all THREE revisions -- 72e67e5e, fac4176c, 2abd9843 -- and advancing the
         pin does not fix it. Recorded in the taxonomy so the hypothesis is not
         retried.
         Also narrowed what it is NOT. cap-call is not an unimplemented form: it
         is present in amu (capability_names, backend/cljs, and
         elaboration-pipeline calls it \"scalar cap-call path first\") and in
         kotoba-native (machine_ir, elf64, peephole, aarch64). So this is not a
         missing lowering reported badly. I tried to surface the underlying
         exception by invoking the nbb entry directly; their print-classpath
         helper returned nothing and the namespace would not resolve. I stopped
         there rather than half-diagnose: going further means working inside amu
         and kotoba-native, which is a different task from this loop, and a
         half-finished diagnosis in this ledger would be worse than none.
         State unchanged and now checked on both halves of the report: CORE
         91/116 78.4% (owner 9, board 8, model-artifact 7, firmware 1), display
         16/16, realtime 0/1 blocked on PLC_RT :compiler. k16 smoke green.
         Nothing here is closable by an agent on this machine. The next real move
         is someone else's: the amu bug, the disk, the token, the board, or a
         TPM-enabled OVMF."}
 {:at "2026-09-10"
  :profile :session-closing
  :core {:emitted 91 :declared 116 :pct 78.4}
  :display {:emitted 16 :declared 16 :pct 100.0}
  :note "Closing entry. What is ON MAIN, not on a branch: PR #324 (89418a77)
         and PR #325 (d97df3cb), both verified by ancestry rather than by the
         API saying merged.
         COVERAGE. 86/116 74.1% -> 91/116 78.4%; display 13/16 -> 16/16.
         Six markers closed, every one case (a) -- a profile that already passed
         whose evidence never reached the measurement. Not one needed a feature
         written. SSH_SESSION, LOADER_RECOVERY, NETBOOT_EMBEDDED, GUEST_INPUT,
         GOP_DISCOVERY, GUEST_SCANOUT_TWO.
         All 25 remaining core markers carry a measured reason: owner 9, board 8,
         model-artifact 7, firmware 1; plus PLC_RT :compiler in an excluded row.
         None is closable by an agent on this machine.
         CI. Two of three jobs had been red on main since 47b81bd. Cause: nbb
         resolves the :deps in nbb.edn by shelling out to `bb ... uberjar`, and
         47b81bd added a :deps coordinate, making babashka a hard requirement of
         every nbb invocation. bb: not found 126 -> 0. aiueos UEFI bare-metal
         smoke red -> green.
         CLJC contract tests is still red, and the honest reading is that fixing
         bb made a backlog VISIBLE rather than creating one. The job now runs to
         completion for the first time: 590 tests / 4089 assertions /
         35 failures / 0 errors across 16 test names (tcb_test x6,
         value_runtime_baseline x4, image_freshness x2,
         smoke_task_registration x2, kotoba_object_reachability, sbom).
         NOT DONE ON PURPOSE. The largest cluster is 13 :digest-drift entries in
         the TCB inventory plus :external-undeclared io.github.kotoba-lang/text.
         Regenerating the inventory would turn several tests green in one
         command. I did not run it. That inventory exists to catch unreviewed
         changes to the trusted computing base, the 13 files were last modified
         2026-09-09 by the owner, and one of them is tcb.clj itself -- the file
         that implements the checking. Blessing them wholesale is the
         make-the-gate-green move this repo warns about, aimed at its own
         security artifact. Review first, then regenerate as the record of it.
         MY OWN ERRORS THIS SESSION, since a ledger that only records successes
         teaches nothing. Three retractions: 'the stop condition is reached'
         (had read only the CORE rows, missing four unclassified markers in the
         excluded ones); GUEST_SCANOUT_TWO classified :code when it was a
         profile; and a fleet-alias fix aimed at the wrong dependency -- I read
         a truncated CI log line (io.github.ko...) and completed it as text from
         context instead of reading deps.edn. The real coordinates were
         kotoba-native, org-nist-sha2, webgpu. Same shape as the grep that once
         truncated input-fail-line=1284 to 128.
         RESUME POINTS. (1) TCB review, above. (2) kbb: it exists and works --
         orgs/kotoba-lang/kotoba/bin/kbb ran an example to exit 0 with
         capability receipts and a working denial path; needs --backend js on
         arm64 since native is x86_64 (SIGILL). Two owner decisions block the
         migration: whether kbb should learn to read kbb.edn (today it reads
         deps.edn, so `nbb.edn -> kbb.edn` is a deletion at the END of migration,
         not a rename -- nbb hardcodes the name and a rename would break every
         nbb script), and which surface goes first (os/aiueos/scripts 45, or the
         superproject scripts 425). CLAUDE.md names an nbb-to-kbb-migration
         skill that does not exist on disk. (3) The four owner-side blockers:
         disk at 99% with 16 GB free (7 markers), MURAKUMO_SERVICE_TOKEN plus
         install intent (9), powering the K16 (8), a TPM-enabled OVMF (1).
         (4) The shared amu checkout is diverged from its west pin and holds
         uncommitted work -- untouched."}
 {:at "2026-09-10"
  :profile :plc-rt-root-cause-fixed-and-emitted
  :core {:emitted 91 :declared 116 :pct 78.4}
  :realtime {:emitted 1 :declared 1 :pct 100.0}
  :note "PLC_RT is emitted. The headline does not move -- realtime is an
         excluded row -- and saying so first matters, because the work behind
         this entry is larger than the number it changed.
         WHERE IT WAS. Not on the emission path. kotoba-native elf64.cljc
         capability-bitmap, reached from exactly one caller, package-user, the
         aiueos CPL3 image route. It did (quot id 8) on an id that is a bigint
         on cljs. The earlier entry had the right exception and the wrong
         location, and the reason is instructive: it bisected by PHASE (check /
         js / native) and concluded x86_64 native emission. Widening to
         neighbouring TARGETS is what named the file -- the same source
         compiles on wasm32, x86_64-linux, aarch64-macos and all three other
         aiueos targets, and that target compiles fine without cap-call. A
         failing case tells you what broke; only a passing neighbour tells you
         where. Six passing neighbours were one command away for a day.
         WHY NO TEST SAW IT. elf64_portable_test has packs-a-user-image, and it
         declares no effects, so the reduce ran zero times: a user image with no
         capabilities was proving the capability path. The added test hands in a
         real bigint and asserts the bitmap BYTE across two ids that share a
         bitmap index, because a coercion that silently lost the value would
         also not throw. Control run both ways on the same tree: without the fix
         the suite exits 1 with the TypeError at elf64.cljc:783 inside quot
         inside capability-bitmap; with it, 68 tests / 638 assertions / 0
         failures.
         LANDED. kotoba-native b88a11b, merged 5f2717c; west pin 50c6480 ->
         5f2717c (root eb5006c2); amu deps edbbe987 -> 5f2717c as
         kotoba-lang/amu#921, which cannot be server-merged because amu main
         requires a PR and two checks. The fix does not reach amu until that
         lands -- deps.edn pins by :git/sha and there is no gate on those.
         MEASURED. Repro exits 0, 8560 bytes. Bitmap bytes 1/2/4 for ids
         16/17/18. Id 20 still refused :kotoba/admission-denied against a policy
         granting 16..19. build-plc-native-program.sh reaches
         AIUEOS_PLC_NATIVE_BUILD_OK with build and reproduction both 8560 bytes.
         Kernel prints AIUEOS_PLC_RT_OK ... capabilities=16,17,18,19.
         SECOND GAP CLOSED. smoke-qemu-uefi.sh defaulted AIUEOS_PLC_ELF to
         build/plc-motor/, which nothing in the tree writes; it now defaults to
         build/aiueos-plc/motor/. The profile could not run unaided before.
         NOT SETTLED, and this is the part not to read as green: the gate is
         INTERMITTENT. Seven runs today, 3 green and 4
         AIUEOS_PLC_RT_FAIL embedded-elf-provider-or-safe-state loader=7 with
         stage= 92, 25, 4, 19 -- a different stage each time, the shape of a
         timing failure rather than a wrong path. Host load1 26-42 with a second
         QEMU running throughout, and the asserted profile is
         release=apic-absolute-ticks cycle=10ticks with deadline among its
         failure modes. A loaded host is the first hypothesis and it has not
         been tested on a quiet one. The marker is reachable and has been
         emitted; the gate is not yet known to be reliable.
         TWO OTHER BLOCKERS MOVED, both outside this repo. Disk: /private/tmp
         held 202 GB in twenty runaway command outputs, none open by any
         process, none touched since 2026-09-09 -- an 80 GB captured lsof and a
         30 GB file of one repeated clojure warning among them. Removed:
         14 GB free at 99 percent -> 204 GB at 79 percent. Model artifact:
         Qwen3.8-27B-UD-IQ3_XXS.gguf fetched by the repo own pinned downloader,
         AIUEOS_QWEN38_ARTIFACT_OK, byte count and SHA-256 both matching the
         pin, at ~/models/aiueos. The seven :model-artifact markers are no
         longer blocked on the artifact; they still need their gates run, which
         another session was already doing when this was written."} {:at "2026-09-10"
  :profile :fresh-reproduction-from-an-empty-build-directory
  :core {:emitted 90 :declared 116 :pct 77.6}
  :note "Not an advance. A reproduction, run from an empty build directory to
         find out whether the 91 on record is something a reader with only this
         repo can obtain, or something that exists because a particular machine
         accumulated the right logs. It is reproducible, and the recipe is the
         value of this entry: one default run gives 66; the fifteen mode
         switches of smoke-qemu-uefi.sh give 76; the seventeen gates that live
         outside those switches give 87; the SSH gates, once the kernel is
         built with AIUEOS_SSH_LISTEN=1, give 90. One short of the record, and
         the missing one is SSH_SESSION -- the live-exchange stall this ledger
         already knows about, which reported AIUEOS_SSH_KEX_REPLY_INCOMPLETE
         stage=2 here.
         FIVE GATES I CALLED RED WERE MY OWN INVOCATIONS. Worth the space
         because each one, left as written, would have entered this ledger as a
         regression. AIUEOS_EXPECT_FAULT and AIUEOS_EXPECT_CRASH only switch
         which assertions run; the variables that inject the fault are
         AIUEOS_FAULT_RECEIPT_SMOKE and AIUEOS_CRASH_RECEIPT_SMOKE, and
         smoke-qemu-journal-recovery.sh already pairs them correctly. Run that
         way, interrupts went 6/9 to 9/9 -- FAULT_RECEIPT, CRASH_RECEIPT and
         PANIC_RECEIPT all emit. The three SSH gates need
         --classpath orgs/kotoba-lang/org-ietf-ssh/src AND a kernel carrying the
         KEX KAT string; both are in their own docstring and I read neither
         until they failed. install and usb-boot need the release image built
         first, which their error message says. Every one of the five announced
         its prerequisite in the failure. The gates were right and the runner
         was wrong.
         TPM2 MEASURED AT A SECOND FIRMWARE, still absent. The taxonomy records
         :firmware on the strength of one OVMF. Homebrew's
         edk2-x86_64-code.fd contains zero TPM strings. Arch's edk2-ovmf
         202608-1 contains them, so it was fetched and passed as OVMF_CODE with
         swtpm attached; the guest still prints
         AIUEOS_TPM2_ABSENT no-root-of-trust-described. So the claim survived a
         firmware built with TPM support, which is more than it had before.
         NOT measured, and the reason this is not yet a diagnosis: whether that
         build actually publishes a TPM2 ACPI table. Strings in a file are not a
         table in a guest, and I did not open the firmware volume to check.
         THE RECLAIM PATH FOR THE 137 GB DATASET WAS ITSELF BROKEN, which is
         this ledger's own class of defect wearing different clothes.
         `nbb manifest/west_annex.cljs annex-drop` -- the documented way to
         return m365-archive's annexed content to B2 -- exits 1 with
         \"Could not find namespace: kotoba.lang.text\": secret-resolve moved off
         clojure.string, and the classpath west_annex.cljs hands b2-creds was
         never updated. Measured both ways: exit 1 with that message before,
         credentials JSON after adding orgs/kotoba-lang/text/src. Fixed in the
         superproject, not here. It fails loudly rather than silently, which is
         the one good thing about it -- I first recorded it as exiting 0 and
         that was my own shell reading the status of the wrong command.
         AND THE TWO COPIES OF THAT DATASET ARE ONE. du reports 137 GB at
         orgs/gftdcojp/m365-archive and 137 GB at github/wt-cs2/m365-archive.
         Same inode, link count 2 on 197 of 200 sampled objects. Deleting either
         path frees almost nothing; the blocks go when both links go. The
         observation was real and the mechanism inferred from it -- two separate
         copies -- was not checked. One stat call decided it.
         CONCURRENCY. Another session moved PLC_RT, reclaimed 202 GB of runaway
         command output from /private/tmp, and fetched the model artifact while
         this ran. Those are theirs. This 90 was measured against 8cbb2dd,
         before the PLC_RT fix landed, and is not comparable to the 91 above it
         line for line -- their union carries physical-K16 evidence this one has
         no access to, which is why the same 25 remaining markers sort into
         different buckets here (board 13, model-artifact 6, owner 4,
         firmware 1, live-exchange-stalls 1)."}]
