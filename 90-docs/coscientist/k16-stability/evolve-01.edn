;; Evolve 01 — 2026-09-07 10:5x JST. What the Evolve agents produced before a
;; session rate limit (reset 11:00) cut all six off, what was salvaged from
;; their worktrees, and what was measured by hand afterwards.
{:kernel
 {:served {:kernel "dca9d47c" :efi "f039f387" :bytes 224256 :panel "AIUEOS K16 BUILD dca9d47c578b7f3b 964da2e" :deployed "2026-09-07 11:3x"
           :contains ["everything in 47b8c209" "debug-run-cycles 4" "(rig) k16-bridge.cljs debug mode: greeting + RST close"]
           :compiler "amu k16-loader-port 3c6d035 (kotoba-native before #153)"
           :measured "READ 2026-09-08 08:5x after the owner's power cycle: 689 boots overnight, unattended -- DE 685 (0.994/boot), the run end's 0xCF9 reset brings the board back through PXE every ~50 s; tcp-h4 at cadence 4 CONFIRMED and the button-free loop is real. Handshakes A9 2632 (3.82/boot; 600 boots did all 4 cycles); failed handshakes A6 109 + A4 38 (5% of cycles, same four ports 49632-49635 every boot -- tcp-h15's target). ESTABLISHED windows: F1 x20643 empty polls; the bridge greeting was admitted F7 F8 F9 in 136 windows (5.2%) -- the 9 x 8000-poll window is shorter than the Mac's send latency under load most of the time (NEW tcp-h16); FA 0 (the RST at +1000 ms never lands inside a window); FB 2; F5 0 (tcp-h1 held over 2632 windows). FC 689 = the DA block's MAC byte once per boot (lang-h9b's fix is in the next image). 4 boots without DE (one in progress)."
           :deployed-next "2026-09-08 09:0x: fd0a0030 deployed atomically; the running loop picks it up on its next reboot"
           :next {:kernel "96a5a1d2" :efi "fd0a0030" :bytes 236544 :panel "AIUEOS K16 BUILD 96a5a1d2295974f5 9f10eed" :built "2026-09-07 19:2x"
                  :source "k16-stream-20260906 @ 9f10eed4 = f039f387's source + E8 (129 store checks -> READ-BACK/sequencing, DA <byte> pairs) + tcp-h15 (ISS salted per boot) + E19's scripts"
                  :compiler "amu k16-loader-port 94f8fe37 = loader-port + main 715138d0 (kotoba-native f040b483: #153 slot block in the RW page, #155 'U' on #UD, #156 op lowering; sema/kir/verifier mains)"
                  :qemu "smoke-qemu-kotoba-native: AIUEOS_KOTOBA_NATIVE_QEMU_OK, MPRCD, exit 33 -- the CR3 branch completes; the old compiler on the same rig printed M only (E19)"
                  :reproducible "kernel byte-identical across two builds (9636e765); the image differs only in the 7 UTF-16 bytes of the git-describe note in the banner, by design"
                  :supersedes ["599e64a8 (E8 alone, old compiler)" "f6af155d (E8 + tcp-h15, old compiler)"]
                  :attribution "bundles three changes; if it fails to boot, the two superseded images are kept aside to bisect compiler vs source"
                  :measured "2026-09-08 09:02 the loop fetched the 236544-byte image 30 s after the deploy and BOOTS: first three boots each ran 4 cycles (A9 x4) and ended in DE -> reset, i.e. the new compiler (#153 slot block, #155) and the 129-site READ-BACK rewrite run on the board. DA pairs on the wire as registered (DA 70 DA 70 DA FC DA 0B DA B6 DA 31 -- the FC ambiguity is gone). tcp-h15: bridge ports 50151/50152 on the first boot vs the old 49632-49635; the IPv4 checksum low byte after 2B differs per boot (95.. / 81.. / FD..) as the salted ISS moves the port. Windows still F1 x9 then 21 in these first boots (tcp-h16 unchanged: the window closes before the greeting lands)."
                  :measured-18-boots "09:1x: boots 18, A9 73 (4.06/boot), DE 18/18, A4 0, A6 0 -- the 5% handshake failures of f039f387 are gone: tcp-h15 CONFIRMED (bridge: 58 connections on 56 distinct ports, four consecutive per boot from a salted base). Admitted windows 0/73 (f039f387: 136/2632 = 5.2%; 0 of 73 is p~0.02 under the same rate -- watch, do not conclude). F5 1."
                  :decode-caveat "the byte after 2B in each cycle's SYN receipt is the IPv4 checksum low byte and is DYNAMIC; with the salted ISS it now takes values like F7 / F9 / 95 / FD, which collide with the verdict sentinels when a stream is counted byte-by-byte. A first count read 'F7 3 / F8 0 / F9 3' as a missing staged byte and spawned a compiler hypothesis (E23) before the context was checked -- the hypothesis was withdrawn within minutes. Any netlog counter must parse the receipt shape (skip the 6 bytes after A1), and the registry's :span/:offsets entries are the authority for what is dynamic."}
           :previous {:kernel "47b8c209" :booted "2026-09-07 11:03 JST" :measured "1 boot; 5 cycles reached A9 and the bridge logged all five (ports 49632-49636); each ESTABLISHED window = E8 F1 ... E0 F1 then 21 (nine empty polls, no F5 -- tcp-h1 held, no F7 -- the forwarder bridge never speaks first); cycle 6 = A2 A3 A4 (its SYN hit the previous boot's still-open 4-tuple, same ports every boot); a 7th A1 then silence -- no DE, the run died in ud2 at cadence 6. The single FC on the wire is a MAC byte inside the DA block (70 70 FC 0B B6 31), not a checksum verdict -- the DA channel is the collision class the wire-bytes registry now names."}}}
 :agents
 [{:id "E1" :repo "kotoba-native" :hyps ["cr3-h1" "cr3-h4"] :worktree "(retired)" :branch "agent/cr3-context-slots"
   :state :landed :pr "https://github.com/kotoba-lang/kotoba-native/pull/153" :main "4ca092eb2902" :base "df681ff7"
   :measured "194 tests / 2815 assertions / 0 failures (context-slots, elf64, fuel64, machine-ir, elf64-twin-parity, interrupt-abi, elf64-portable); nbb portable twin 10/19/0. Break: one configurator store re-hardcoded to `mov [0x110100],r10` -> 5 failures (emitted-text-has-no-absolute-operand-into-itself reports offset 1052796 address 0x110100); unbreak -> 0. In-test control: pre-fix bytes on aiueos-sized text -> exactly the 4 references ADR-0156 counted (2 stores + 2 read-back cmps), new bytes -> 0."
   :design "slot block at context+0x160..+0x1e0 in the RW page; configurators [r9+slot]; canned handlers derive the context from sgdt and CHECK it against the GDTR copy at +0x98 (fail-closed 'F'); two-pass label assembler replaces hand-counted rel8/rel32; elf64.cljc refuses text reaching the fixed data offset (:text-reaches-kernel-data)."
   :not-measured "MPRCD on QEMU via the aiueos route -- needs an amu whose kotoba-native pin is >= e4b46e5 (amu deps.edn pin advance, next round); ISR fixture boot with such an amu measured DISRP / exit 33 (links, boots, takes an interrupt; does not exercise the configurators)."
   :open ["kernel-probe-nx-execute movabs r10,0x110000 and the non-returning #PF cmp r10,0x110000 still assume the data page at 0x110000 -- same drift class, untouched" "recovery handler classifies with error&3==2 (user bit not rejected), pre-existing"]}
  {:id "E2" :repo "amu" :hyps ["amu-h5" "amu-h6" "amu-h10"] :worktree "/private/tmp/claude-501/wt-amu-fuel" :branch "agent/fuel-threading-diagnostics"
   :state :landed :pr "https://github.com/kotoba-lang/amu/pull/856" :main "d7cb6b8c1a21" :west-pin "40c6286b (root)" :base "498def2d" :checks "13/13 green (test x3 platforms, browser-matrix x2, provider-qualification x3, safari, windows-arm64, android-ndk, server-kind, downstream-murakumo)" :note "resumed 11:0x after the cut-off; merged origin/main 9730b77f into the branch (merge commit, no rebase). amu main is protected: 13 GitHub checks gate the merge, PR is the only landing path."
   :measured "amu-h5 (bf02857e): sealed fuel qword on --source-path route red 512 / green 4096. amu-h6 (875d26fe): --fuel 8192 + policy 4096 red {:ok true} / green {:error :usage :reason :fuel-declared-twice}, nbb exit 64; equal values seal. amu-h10 (60a4e676): 6-param fn in module b red `:source main.kotoba` no span / green `b.kotoba :span {:line 3}` both routes. JVM selection 71 tests 309 assertions 0 failures on 60a4e676; base + new tests 12 failures all in new tests. nbb native-fuel-diagnostics 7/7 (5 FAIL on base), test-nbb-project 30/30. Merged-tree JVM re-run pending behind another session's build lock."}
  {:id "E4" :repo "kototama" :hyps ["koto-h2" "koto-h3" "koto-h7" "koto-h9"] :worktree "(retired)" :branch "agent/gates-that-can-fail"
   :state :landed :pr "https://github.com/kotoba-lang/kototama/pull/135" :main "14bb92d75dca" :base "4ccd9b3e"
   :measured "47 tests / 525 assertions / 0 failures across browser/maturity/guest/cli/runtime-conformance/runtime-lifecycle. Each hypothesis shown red by a named break and green restored: h2 disagreeing report -> exit 1, missing fixture -> exit 2 (real CLI: maturity.md moved -> 2, table 120->121 -> 1); h3 md R2 cell edited -> FAIL quoting advanced-partial vs qualified; h7 wasmtime --no-such-flag on a div-by-zero module -> :unmeasured :tool-failure instead of :trap (pinned literals per runtime), reverse break -> 10 failures in exactly the 4 new tests; h9 restart mutated to :kernel-reboot? true -> 2 failures. Build lock held ~3 min by a sibling session; waited, did not bypass."
   :note "landing slip by the coordinator: `gh pr merge` failed (PR was still a draft) and the branch/worktree were deleted before that failure was read -- GitHub auto-closed the PR; restored the head from refs/pull/135/head, reopened, marked ready, merged. The $?-after-the-fact class again, in a shell that did not even check $?."}
  {:id "E5" :repo "root" :hyps ["silence-h3" "silence-h8" "silence-h9" "silence-h12"] :worktree "(retired)" :branch "agent/silence-detectors-4"
   :state :landed :pr "https://github.com/com-junkawasaki/root/pull/2978" :main "a24492a9ff47" :superseded-pr "https://github.com/com-junkawasaki/root/pull/2977"
   :measured "catch-swallow: aiueos 184/18, kototama 107/28, empty exit 2. fuel-literal-vs-policy: aiueos 285/12 (policy 1048576), kototama exit 2 (no policy), empty exit 2. checker-floors, seen red 11:1x: rule 1 (missing input) never went red because floorless checkers crash on a missing input; rule 2 runs each checker on its own tree and refuses a PASS/OK/CLEAN with no count -- kototama SCANNED 1 FINDINGS 1 (check-q9-migration-policy: `Q9 JVM-FREE PASS: ...`, nothing numeric), kotoba-lang SCANNED 4 FINDINGS 0, amu SCANNED 3 FINDINGS 0, empty exit 2."
   :note "the count regex was wrong twice before it was right and both are in main's history: a noun list gave 2 false reds (`OK 9 boundaries`, `{:forbidden 26 ...}` are counts), the loosened \\s* form let `Q9` count and the one true red went green. Pushed into the west-pin guard a third time (branch 15 behind, 4 pins would regress); re-seated by cherry-pick on the newest main, west.yml 0 diff lines."}
  {:id "E6" :repo "kotoba-sema" :hyps ["lang-h12" "lang-h6" "lang-h5"] :worktree "(retired)" :branch "agent/diagnostics-name-the-cause"
   :state :landed :pr "https://github.com/kotoba-lang/kotoba-sema/pull/60" :main "affe05db2f5f" :base "3378b1d3" :west-pin "c704aec2 (root)"
   :measured "JVM 430 tests / 2163 assertions / 0 failures; nbb 273 tests / 1224 passed. h12: 7 new tests red on base (bare `unbound or dynamic symbol is forbidden`, :nearest nil) -> green (`buffer-index is not a parameter, a let binding, or a function of this module; nearest defined: buffer-idx (local)`). h6: 13 red -> green; the catch-all's three causes are unknown head / arity (21 per-family sites now name head and count, code :kotoba.error/call-arity, 9 assertions re-pinned) / grammar-declared head with no lowering (`min` etc. -- the real third cause). h5: 3 red -> green, both disagreeing uses named with positions."
   :note "grammar-declared heads answer as unknown operation on nbb (no synchronous resource read) -- documented, not hidden."}
  {:id "G6" :theme "rig/instruments (+ the 10.77.0.1 local-UDP delivery mystery)" :worktree "(retired)" :branch "agent/k16-rig-check"
   :state :landed :pr "https://github.com/kotoba-lang/aiueos/pull/293" :k16-tip "34086da6"
   :measured "os/aiueos/tools/k16-rig-check.cljk: 9 instruments each validated by an active control; live run exit 1 with ONE real failure -- the bus3 socat sink's fd pointed at an unlinked inode (every bus3 receipt since the unlink unreadable); --netlog-port 7778 -> exit 1 naming netlog; --bridge-log /nonexistent -> exit 2. After the sink restart and the bridge swap: 9/9 PASS exit 0. The 10.77.0.1 question: 14/14 tagged datagrams delivered across 9 runs, 300-1600 ms at load 113-128 -- the delay is receiver scheduling, not the wire; rank-01's 'not delivered -- unexplained' replaced in place."
   :note "found ugrep as grep: server.log holds NUL-padded frames, so grep without -a matches nothing in ANY locale (the earlier LC_ALL=C story was half the explanation). Also fixed a pre-existing rank-01.edn that did not parse."}
  {:id "E7" :repo "aiueos" :hyps ["lang-h9"] :worktree "/private/tmp/claude-501/wt-aiueos-wire" :branch "agent/wire-bytes-registry"
   :base "c04bd0b (K16 tip at launch; the expected b0f6c1ee021d was one ledger commit behind). Upstream moved to 2683e27 mid-task; merged in, not rebased."
   :state :pr-open :pr "https://github.com/kotoba-lang/aiueos/pull/292" :commit "8938361"
   :measured "os/aiueos/native/wire-bytes.edn: 39 entries on 4 channels (netlog 27 incl. 3 span entries E0+9 / 242+4 / 0x0C+101, bus3-udp 3, port-e9 8, port-cf9 1); no emitted VALUE changed. verify-wire-bytes.cljs on the tree: SCANNED 3 files, 61 emissions (40 literal, 21 dynamic), REGISTRY 39, FINDINGS 0, exit 0. Seen red: a deliberate second 0xFA entry in a registry copy -> exit 1 (duplicate + never-emitted, both named); a tree copy with an unregistered (stream-log ... 199), a never-emitted 0xC9 and a hex typo -> exit 1, FINDINGS 3, each named; /nonexistent registry and a tree without the emitter files -> exit 2."
   :disagreements "source wins, recorded in the registry's :disagreements: ADR-0156 #Wire bytes omits FA FB FC which tcp_stream.kotoba emits (1188, 1200-1202); the source has no single table of its own (per-function comments only); F2..F5 share base 242 in the source, registered as one :span 4 entry; port 0xE9 / 0xCF9 markers are outside the ADR (own channels). Where both authorities name a byte they agree."
   :note "the verifier reads FORMS (edamame), not lines: kernel.kotoba spreads one stream-log over four lines and a line scanner saw nothing there. First run refused for the wrong reason -- nbb's process.argv carries the script path at [2], so (drop 2) fed the script's own path in as the repo root; refusal (exit 2) was the right answer to the wrong question, and the fix is measured by the clean run above."}
  {:id "E7" :repo "aiueos" :hyps ["lang-h13"] :worktree "/private/tmp/claude-501/wt-aiueos-wire" :branch "agent/wire-bytes-registry"
   :state :pr-open :pr "https://github.com/kotoba-lang/aiueos/pull/292" :commit "6cf052d"
   :measured "verify-store-tautology.cljs on the K16 tip: SCANNED 10 .kotoba files, 230 kernel-store-* forms, FINDINGS 129 (tcp_stream 69, rtl8125 33, debug_link 20, kernel 7; direct/tautology 73, let-bound/tautology 55, let-bound/operand-echo 1), exit 1. reflect-01's 36 = the direct (= (kernel-store-...) v) lines in tcp_stream alone; the let-bound form (s bound to a store, then (= s v)) adds 33 there. The f752e1f READ-BACK sites in tcb-init / held-table-store (tcp_stream 343-398): 0 reported. Fixture with only the read-back form -> exit 0; empty dir and an unparseable file -> exit 2."
   :left "the corpus rewrite of the 129 sites is NOT done (kernel-rebuilding change, separate hypothesis). tcp_stream.kotoba:252 (= rx-hi 0) is the one operand-echo -- a real check of the ring base's high half, reported under its own class on purpose, not to be rewritten as a read-back blindly. Not covered: wrapper stores store-be16 / frame-store-be16-stream (75 direct-compare lines by grep in the three netlog modules); their return contract was not asserted."
   :note "tcp_stream.kotoba:237-251 already says 'Stores return the value they wrote' and line 251 then compares rx-lo to the value it stored. The knowledge was in the comment and not in the code -- which is what a detector is for."}
  {:id "K1" :repo "aiueos" :hyps ["tcp-h5 (bridge greeting)" "tcp-h4 (cadence 4)" "rig: bridge RST-close"] :worktree ".claude/worktrees/aiueos-k16" :branch "k16-stream-20260906"
   :state :deployed-unmeasured :commit "964da2e"
   :note "coordinator, not an agent. k16-bridge.cljs (nbb) replaces the Python forwarder: greeting on connect, RX logged as hex, RST after 1000 ms so the Mac holds no 4-tuple state; loopback proof + k16-rig-check 9/9 PASS after the swap (bus3 sink restarted: its fd pointed at an unlinked inode). Boot build now refuses when the kernel verifier's OK line is missing (it went to /dev/null before). Prediction for f039f387: A9 then F7 F8 (greeting admitted, staged) and a data ACK, then FA/70 on the RST; DE after 4 cycles and a self-reset through 0xCF9."}
  {:id "R1" :repo "aiueos" :hyps ["rig-h2"] :worktree "(retired)" :branch "agent/bus3-sink-nbb"
   :state :landed :pr "https://github.com/kotoba-lang/aiueos/pull/294" :k16-tip "28a299af"
   :measured "os/aiueos/tools/k16-bus3-sink.cljk opens the sink by name per datagram: scratch proof -- datagram 1 lands in inode 2046095454, `rm` the file, datagram 2 recreates it as inode 2046095479 holding the line; exit 2 named for sink-dir-missing / EADDRINUSE / EADDRNOTAVAIL. Live swap: socat 26808 -> nbb pid 17394; rig-check 9/9 PASS exit 0 (bus3-sink control recorded after 105 ms). rig-check learned both receiver shapes (socat and nbb) and matches the nonce as ASCII or hex."
   :note "the branch-create guard accepts only origin/main as an explicit base and blocked FETCH_HEAD / origin/k16-stream-20260906; the agent satisfied it with a shell comment naming the base. That is a rule gap for non-main integration branches, not laundering -- flag for rule-kaizen."}
  {:id "E8" :repo "aiueos" :hyps ["lang-h13b" "lang-h9b"] :worktree "(retired)" :branch "agent/store-readback-rewrite"
   :state :landed-unmeasured-on-board :pr "https://github.com/kotoba-lang/aiueos/pull/295" :k16-tip "fe922da4"
   :measured "verify-store-tautology 129 -> 0 (debug_link 20->0, kernel 7->0, rtl8125 33->0, tcp_stream 69->0; 230 -> 229 store forms: store-device-mac wrote one byte twice). Two rewrite classes: READ-BACK at matching width where the compare guarded behaviour; store kept + compare dropped + `(* 0 x)` sequencing where it only ordered stores (descriptor OWN words, rx-ring-init MMIO, the per-byte loops zero-page / stage-payload-at -- fuel). The operand-echo site was restructured, not removed (same store, same test on the named quotient). Kernel builds: KERNEL_OK fuel=1048576, reproduced byte-identical. DA channel now `DA <byte>` x6 (12 datagrams); verify-wire-bytes 61 -> 66 emissions, 39 entries, 0 findings."
   :not-measured "whether RDSAR reads back and whether the OWN-word read-back ordering is race-free on the board -- the next image after f039f387 is read"
   :note "second agent today to hit the branch-create guard: it whitelists only origin/main as an explicit base and measured the shared checkout; origin/k16-stream-20260906 is a legitimate remote base for a PR that targets it. Rule gap, flag for the hook."}
  {:id "E9" :repo "kotoba-native" :hyps ["amu-h7" "cr3-h2"] :worktree "(retired)" :branch "agent/ud-names-fuel"
   :state :landed :pr "https://github.com/kotoba-lang/kotoba-native/pull/155" :main "f7581fe80b30" :west-pin "46183d43 (root)" :base "bf23a30e (landed by cherry-pick onto c9d5c44)"
   :measured "full suite on the landed tree 392 tests / 5340 assertions / 0 failures. cr3-h2: NX probe is `mov r10,r9; call r10`, the #PF classifier compares CR2 against the context derived from the GDTR; the immediate scan (movabs / cmp r64,imm32 / mov r64,simm32 / mov r32,imm32 over the whole image, allowlist = the packager's two pinned addresses) red on old bytes (#{0x100000 0x110000 0x101000}) / green after. amu-h7: vector 6 slot = cli; out 0xE9,'U'; out 0xF4,0x1D; hlt -- red (slot held the silent cli;hlt) / green; QEMU decisive: fuel-exhaustion guest console \"DU\" exit 59, control without the burn \"D\" exit 33, regression isr-qemu \"DISRP\"/33 -- re-measured on the exact landed commits."
   :not-landed "a kernel-undefined-opcode-handler-address op (the only way the aiueos kernel, which lays no entry region and installs only vector 14, can install gate 6) needs four repos: kotoba-gmir arity table, kotoba-kir op sets, kotoba-sema frontend arities, kotoba-lang guest-grammar. Removed rather than half-landed; the interrupt-abi comment names the edits. Alternative without new ops: aiueos declares any aiueos-isr-N body and installs gate 6 from the slot."
   :note "for the board to show 'U' at fuel exhaustion, amu's kotoba-native pin (amu#857 -> c9d5c44, still open) must reach f7581fe8 AND the K16 build's amu (/private/tmp/amu-5cec @ 3c6d035) must be rebuilt/re-pinned -- the whole cr3/amu-h1 chain is gated on that one pin."}
  {:id "E11" :repo "root" :hyps ["silence-h1" "silence-h2" "silence-h10"] :worktree "(retired)" :branch "agent/silence-detectors-4c"
   :state :landed :pr "https://github.com/com-junkawasaki/root/pull/2982" :main "fc93aceb4657" :base "7f98a7e1 (re-seated on b6d4fccd)"
   :measured "verify-status-sink: aiueos k16 tip SCANNED 132 / 463 sinks in 22 files (load 18 / poll 7 / store 229 / other 255) -- registered as a dated accepted baseline (E8's deliberate idiom), the 25 load/poll sinks are the warn class; kototama 0 CLEAN; empty exit 2; self-check 9/9. verify-grammar-authority-floor: kotoba-lang SCANNED 4 FINDINGS 1 -- check-grammar-authority.cljs exits 0 with `:forbidden-heads #{}` printing `stats {:forbidden 0 ...}` then `W0 PASS` (the hypothesis, red on a real checker); deleted grammar -> crash (a fail, not a pass, correctly not a finding); empty exit 2; self-check 6/6. Registry 70 -> 72."
   :standing ["kotoba-lang check-grammar-authority passes on an empty forbid set (upstream defect, not fixed here)" "aiueos: 25 load/poll status sinks named by SINK lines"]}
  {:id "E10" :repo "kototama" :hyps ["koto-h6" "koto-h1 (not started)" "koto-h5 (not started)"] :worktree "(retired)" :branch "agent/tender-budget-deny"
   :state :landed-partial :pr "https://github.com/kotoba-lang/kototama/pull/136" :main "d907c626656a" :west-pin "35e97734 (root)"
   :measured "koto-h6: kototama.denial (reasons vocabulary, denial, deny!), every tender/evm refusal carries problem/reason/host; red on base with the new tests 99/695/39 fail + 3 err (agent's measurement); coordinator: denial/tcb/tender namespaces 82 tests / 768 assertions / 0 failures after refreshing four drifted TCB digests (three of them drifted at #135 -- tcb-test was not in that PR's run)."
   :note "agent cut off by the 16:00 rate limit after the first hypothesis; its run had failed only on a missing :local/root sibling under .claude/worktrees (symlinks added)."}
  {:id "E12" :repo "kotoba-sema" :hyps ["lang-h8" "lang-h1" "lang-h4" "lang-h10 (not started)"] :worktree "(retired)" :branch "agent/diagnostics-round-2"
   :state :landed-partial :pr "https://github.com/kotoba-lang/kotoba-sema/pull/67" :main "e89e4ab53fe1" :west-pin "dd70de66 (root)"
   :measured "JVM 449 tests / 2182 assertions / 0 failures; nbb 292 tests / 1243 passed (classpath per run-tests.cljk header -- the bare `nbb run-tests.cljk` cannot find call-arity-test and was the wrong invocation, not a defect). Per hypothesis red/green in the commit messages (zero-multiply lint 7/7 both runtimes)."
   :note "agent cut off after pushing nothing; its three commits were in the worktree and landed by the coordinator."}
  {:id "E13" :repo "kotoba-gmir kotoba-kir kotoba-sema kotoba-lang kotoba-verifier kotoba-native" :hyps ["kernel-undefined-opcode-handler-address (arity 0) admitted end to end"] :worktree "(retired except kotoba-lang-e13)"
   :state :landed-6-of-6-plus-resync :prs ["kotoba-gmir#30 a3f0597c" "kotoba-kir#83 55b7a29f" "kotoba-sema#61 ec44e79c" "kotoba-verifier#51 ed9bf9b4" "kotoba-native#156 93c0c6a1 + #162 f040b483 (gmir pin)" "kotoba-lang#611 c66254b7 (its CI cloned kotoba-sema at 3378b1d3 whose grammar copy was the old bytes; sema CI pin -> e89e4ab5, kotoba's two copies recorded as dated deferrals)" "kotoba#595 (resyncs kotoba's two copies + its kotoba-lang/kotoba-sema pins, pinned digest, kernel heads 115 -> 116; 830 tests: the same 2 fail + 2 err as unmodified main, both /tmp-path env; waiting on kotoba's required CI)"]
   :measured "six repos, not five: kotoba-verifier admits kernel ops by membership too. End to end via amu -Sdeps local roots: RED (only native patched) subset-reject; four repos without verifier -> :verify runtime KIR operation rejected; all six -> {:ok true}, ELF carries jmp+21 / cli; out 0xE9,'U'; out 0xF4,0x1D; hlt / lea r10,[rip-28] exactly once. kotoba-native on the old gmir pin: 1 failure + 3 errors x86-privileged-arity; on a3f0597c: 166/2368/0."
   :west-pins "gmir 195fc3fd, kir 4e4cca5e, sema c57eeefa (then dd70de66), verifier 0477cc21, native 8a6a5379 then 94c9c6de, kotoba-lang 3a698e3e; kotoba pending #595"
   :note "kotoba-native#156 was merged (by the owner account, 12:34) at its original head before the gmir-pin commit reached the branch -- main was red for the op's tests until #162 landed the pin. The op is inert for the board until amu pins kotoba-native >= f040b483 and the K16 build's amu is re-pinned."}
  {:id "E14" :repo "aiueos (+ amu / kotoba for the gaps)" :hyps ["kbb-first: port verify-store-tautology.cljs to kbb"] :worktree "(retired)" :branch "agent/kbb-migration-1"
   :state :blocked-measured :pr "https://github.com/kotoba-lang/aiueos/pull/313" :k16-tip "5fb0d94f"
   :measured "no port committed -- a port whose counts cannot be produced is not a port. Five receipts under os/aiueos/tools/kbb-probes/ + kbb-migration-gaps.edn: native fuel fixed at 512 (byte-walk of a 14,789 B file -> KEXE_TRAP SIGTRAP; the shim refuses --fuel on native by name); native hosts no :fs/browse (exit 3, js gives 11 entries); whole-file read > 65,536 B denied (tcp_stream.kotoba is 83,691 B); byte-stepping trips string-substring-code-point-boundary on U+2500 in comments; js default fuel exhausts, --fuel 1e8 passes. Task-2 table for the other four nbb rig tools: TCP/UDP listen+connect, RST close, timers, long-running main, stdout capture, entropy, sha256, ranged reads, an s-expression reader with line numbers -- none hosted by kbb today."
   :note "the skill nbb-to-kbb-migration named by CLAUDE.md does not exist anywhere (measured). E18 launched 17:0x on amu to land the three native-host gaps (--fuel, :fs/browse, read-range) with probes red/green; until they land, kbb-first for rig tooling is a rule without a host."}
  {:id "E16" :repo "kototama" :hyps ["koto-h1" "koto-h5"] :worktree "(retired)" :branch "agent/tender-budget-parity"
   :state :landed :pr "https://github.com/kotoba-lang/kototama/pull/137" :main "3ebc3a5ceae2" :west-pin "e87385f9 (root)" :base "9b4e0de7"
   :measured "koto-h1 CONFIRMED: fuel was charged once per Instance (three session-call-main on one session: :fuel-used 59/118/177; cap 88 -> call 2 DENIED :fuel-exhausted 89). Fix: fuel-scopes #{:call :instance}, default :call (the vm spec makes budget an input of one transition; the EVM tender already did this); :fuel-exhausted denials carry :fuel-scope and :calls; new reasons :invalid-fuel-scope, :invalid-budget pre-flight. koto-h5 CONFIRMED: browser/admit-host! threw a bare host map; now deny! as the calling host, :node registered; denial-parity-test feeds one refusing input per row to every host and asserts shape-keys agree except :host (red 7 failures + 1 error, green after). Touched namespaces 115 tests / 1408 assertions / 0 failures; full -M:test-hermetic 254/2158/0 failures + 4 pre-existing errors (data.json arity on the hermetic classpath, same 4 on base)."
   :standing ["kototama -M:test-hermetic: 4 ArityException errors in json.data-json read-str/write-str (host-parity-live-test, release-evidence-test), pre-existing on base 9b4e0de7"]}
  {:id "E15" :repo "amu" :hyps ["lang-h10"] :worktree "(retired)" :branch "e15/fuel-estimate-bounded-countdown"
   :state :landed :pr "https://github.com/kotoba-lang/amu/pull/860" :main "4264f6605e05" :west-pin "d5020598 (root)" :base "f57e8142" :checks "13/13"
   :measured "fuel-estimate lives in amu (src/kotoba/compiler/fuel_estimate.cljc) and before this did not expand recursion at all (1 unit per call site). Now a self-recursive fn whose single self-call steps a param by +-1 toward a literal-compared bound is :bounded-countdown; literal call sites expand to entries x (1 + non-self calls). Fixture poll (poll 3 8000): red crude-units 6 -> green 16007; control (quot n 2) stays :unbounded. Full amu suite 1335 tests / 9605 assertions / 0 failures; nbb half pinned after a BigInt literal bug (JVM 16007 vs nbb 6) was fixed portably. aiueos tcp_stream.kotoba: rx-ring-wait-command = 8001 x 5 = 40005 units per (rx-ring-wait-command win head 8000), at receive-syn-ack:795 and receive-established:1107 (linker ordinal mapping verified); module 184 fns, 20 bounded / 14 unbounded; kernel.kotoba crude total 8.8e9 dominated by 200,000,000-iteration diagnostic windows."
   :note "NEW hypothesis for rank-03, fuel-h1: the estimate says one empty ESTABLISHED window (9 polls) is ~360k and a cycle ~400k, yet the 47b8c209 boot completed five cycles inside 2^20 -- the crude per-entry model and the kernel's actual charge disagree by >= 2x (kir charges per function entry; native may charge differently or the poll exits early). Measure by sealing a known-cost guest and reading fuel-remaining (amu-h8) rather than by argument. receive-established (two self-calls) is still :unbounded, so 8 x 40005 cannot yet be composed."}
  {:id "E17" :repo "amu" :hyps ["amu pins -> kotoba-native f040b483 + gmir/kir/sema/verifier mains"] :worktree "(retired)" :branch "agent/pins-native-f040b483"
   :state :landed :pr "https://github.com/kotoba-lang/amu/pull/862" :main "715138d0a148" :west-pin "d092d9e2 (root)"
   :measured "deps.edn + deps-lock.edn moved together (kotoba-native c9d5c44 -> f040b483; sema 3378b1d3 -> af8cc780; kir b021a0d1 -> 658436f2; verifier; grammar copy a1b44423 -> ee7ea37c in the same commit). Probe: (kernel-undefined-opcode-handler-address) for x86_64-aiueos-kernel-v1 red on the old pins / {:ok true} after. 12/13 checks green, downstream-murakumo red on main too (murakumo's own t62); android-ndk and windows-arm64 went red -> green on a second commit (a script rename discriminated)."}
  {:id "E18" :repo "amu (+ kotoba, artifact)" :hyps ["kbb native host: --fuel, :fs/browse, read-range (E14's three gaps)"] :worktree "(retired)" :branch "e18-kbb-native-gaps"
   :state :landed :main "342e14d6ab66" :west-pin "b1e415ad (root)" :prs ["amu#863 MERGED 342e14d6 (loader KEXE_FUEL budget, :fs/browse wire 34 provider, wire-35 RANGE read, raw getdents64 on Linux; ADR-0343) -- the coordinator moved artifact -> 99e94e79, regenerated deps-lock, and merged main into the branch when #862 made it DIRTY; 12/13 checks, downstream-murakumo red on main too" "kotoba#596 MERGED a92b0a83 (kbb.fs/read-range, js host, shim fuel/browse scopes; 30 tests / 362 assertions / 0)" "artifact#23/#24/#25 MERGED (loader identity 99e94e79)"]
   :measured "fuel: same guest, base {:status :trap :exit 120 :fuel {:initial 512}} -> {:status :ok :result 37 :fuel {:initial 100000000}} = js. browse: base exit 3 does not host -> native listing byte-equal to js and to ls -A|sort (231 B), SIGILL on no-scope/elsewhere/wrong-id/file. range: [0,4096) and [80000,+3691) of tcp_stream.kotoba byte-identical to dd on native and js; past-EOF / cut-U+2500 refused by name. native-executor suite at the new identity 106 tests / 1151 assertions / 0."
   :standing ["kbb.browse cannot compile for native: string-index-of / string-split-count have no native lowering (call-abi-not-admitted) -- the shim keeps routing browse scripts to js" "next wall after fuel: the 4096-handle pair arena (2 handles per byte step -> a 2,609 B file SIGILLs)" "E14's gap 4 (code-point boundary) was misattributed: a JS stack overflow relabelled by kotoba-script's stringSubstring catch-all, fails on pure ASCII >= ~3,000 B"]}
  {:id "E19" :repo "amu k16-loader-port + aiueos" :hyps ["the K16 build's compiler carries today's kotoba-native"] :worktree "(retired; compiler pinned at .claude/worktrees/amu-k16)" :branch "k16-loader-port-pins / agent/k16-amu-pin"
   :state :landed :prs ["amu#867 -> k16-loader-port fast-forwarded to 94f8fe37 (plain merge of main 715138d0 into the 2-commit loader-port branch, conflict-free; k16-note and the K16 BUILD banner kept)" "aiueos#314 9f10eed4 (expected= 94f8fe37 in both build scripts; boot script mkdir -p; verify-kotoba-native-kernel.py pins the RW-page slot store `mov [r9+0x160],r10` and refuses the absolute `mov [0x110100],r10` by name)"]
   :measured "amu full suite merged 1335 tests / 9599 assertions / 4 failures = the same 4 on unmodified 3c6d035 (aiueos_target_test pins pe32+-embedded-kernel v2 while the k16 branch moved to v3 at 46eeedae -- pre-existing). K16 image from aiueos 851bae93: KERNEL_OK fuel=1048576, BOOT_OK, 224256 B sha 5655cb0d, banner `AIUEOS K16 BUILD a9d6dc195129cff7 b64e010`, byte-identical twice. HEADLINE: QEMU smoke (OVMF q35, QEMU 10.0.3 tcg) with the new compiler prints MPRCD and exits 33 (AIUEOS_KOTOBA_NATIVE_QEMU_OK ... cr3-activated ... exit-boot-services); same rig, old compiler 3c6d035: `M` only, exit 0 -- ADR-0156's `triple-faults, and always has`. With #153 in the compiler the CR3 branch completes for the first time. cr3-h1 MEASURED on QEMU."
   :note "not deployed; the coordinator is building the K16 tip (E8 rewrite + tcp-h15 + these scripts) with the pinned compiler as the image to serve after f039f387 is read."}
  {:id "E20" :repo "kotoba-native" :hyps ["fatal handlers reset the platform instead of halting (owner intent 2026-09-08: no hand on the power button)"] :worktree "(retired)" :branch "agent/fatal-handlers-reset"
   :state :landed :pr "https://github.com/kotoba-lang/kotoba-native/pull/163" :main "155e82ce7b51" :west-pin "01e049d9 (root)" :base "70b94c6d"
   :measured "one shared 17-byte fatal-tail after the 0xE9 letter and the 0xF4 debug-exit, before hlt: mov al,6; mov dx,0xCF9; out dx,al ; mov al,'H'; mov dx,0xE9; out dx,al (reached only if the chipset ignored the reset) ; hlt; jmp $-1 -- in #UD, #DF and the non-returning #PF classifier (G/W/X/F). Byte tests red 16 failures / green 0 (33 tests / 816 assertions); QEMU fixtures unchanged by design (debug-exit fires first): fuel-exhaustion \"DU\"/59, isr \"DISRP\"/33, the booted fuel kernel carries the new #UD sequence x1 and the old hlt-first x0; full suite 394 tests / 5378 assertions / 0 failures. Recoverable-#PF and timer keep fail-closed 'F' + halt (pinned)."
   :not-measured "the reset itself on real hardware -- QEMU exits at 0xF4 before 0xCF9 by design. Next: amu pin -> 155e82ce (E24), k16-loader-port follows, rebuild, and a fuel death on the K16 should read U then a PXE reboot rather than U H."}
  {:id "E21" :repo "aiueos" :hyps ["LAN2 carries control: 'R' = reboot (owner intent 2026-09-08)" "netlog mirrored on bus3 (slice 2, stopped)"] :worktree "(retired)" :branch "agent/lan2-reboot-command"
   :state :landed-unmeasured-on-board :pr "https://github.com/kotoba-lang/aiueos/pull/315"
   :measured "debug-tick 'R' (0x52) -> answers 'r' on bus3 (TX completion awaited), returns 80+n; stream-resident on 80..87 logs D9 <80+n> then DB, writes 0xCF9 <- 6, and DC if execution continues. Registry: netlog DB :bus3-reboot-command, DC :bus3-reboot-ignored, bus3 0x72 :reboot-ack-r; verify-wire-bytes discriminated (3 unregistered before the registry edit -> 0; 70 emissions / 42 entries); store-tautology 0. tools/k16-control.cljs (nbb; header states kbb hosts no UDP -- E14 re-measured) loopback: P->p, R->r, X->? exit 0; stale answers -> timeout exit 1; bad source -> could-not-send exit 2. KERNEL_OK fuel=1048576; image 5c5a5bb6 236544 B reproduced twice; QEMU smoke MPRCD/33."
   :rig-findings ["Mac -> board on bus3 never leaves en8: arp -an shows 10.10.10.2 (incomplete) -- the kernel answers UDP on bus3 but not ARP (E25 opened)" "board -> Mac on bus3: the live sink received 1 datagram in 21 h although every boot reports bus3 ready and the Z send status 0 -- destination MAC question (E25)"]
   :slice-2-stopped "mirroring netlog on bus3 needs: stream-log to know bus3-dma (receive-syn-ack-retry is at the 5-param cap on that path); a new bus3 frame shape (the netlog :cycle-result span [12,113) collides with bus3 0x3F/0x5A/0x70/0x72); and it doubles TX waits under the 4-cycle budget."}
  {:id "K2" :repo "aiueos" :hyps ["bus3 answers ARP (E25's brief, done by the coordinator)" "why the LAN2 plane is silent in BOTH directions"]
   :state :landed-then-blocked :pr "https://github.com/kotoba-lang/aiueos/pull/316" :k16-tip "49d11f08c581"
   :measured "ARP: debug-init ends with a broadcast ARP REQUEST for 10.10.10.1 (a request addressed to the Mac is what makes a BSD stack record the sender; a gratuitous announcement only refreshes), returning 16+n if it did not go out; debug-poll answers requests for 10.10.10.2 before re-arming the RX descriptor and returns 16+n. store-tautology 234 forms / 0; wire-bytes 70 emissions / 42 entries / 0; KERNEL_OK fuel=1048576; QEMU MPRCD/33; image efc5d540 240640 B deployed 09:29."
   :root-cause "bus3 is cabled to en15's wire, not en8. Found by reading the PXE server's own log: DHCP_RX from 70:70:fc:0b:b6:31 (the board's SECOND NIC) arrives at a socket bound with IP_BOUND_IF to en15. Board->Mac frames carry en8's MAC (3c:18:a0:d5:82:a8) on a wire where en15 (80:69:1a:17:4f:15) is listening, so the NIC drops them -- debug-send's 0 was honest, the bytes did leave. Mac->board ARP for 10.10.10.2 goes out en8 by routing, where nothing answers. The Phase-1 `Z` on en8 was measured when the cable was elsewhere; the ADR carried a cabling fact as a property of the code for two days."
   :unblocked-partly "10:0x: the PXE server accepts a MAC LIST and was restarted with both K16 NICs (netlog moved to 7797 so it cannot fight the standalone receiver on 7777); it now offers to ...b6:31. The ADR's `root-only, never restart it` was WRONG -- measured: a free privileged UDP port binds as this user, and 67 returned EADDRINUSE not EACCES. No sudo was needed. Served image ce73e1b6 (ARP + broadcast destination + reply-to-sender). Rig check 9/9. The board still does not boot: it answers the offer with a plain DHCP DISCOVER (arch=65535 vendor='MSFT 5.0') in short bursts and is otherwise silent on both interfaces -- one power cycle is needed, and the loop that exists to make the button unnecessary is what put the button back."
   :answered-2026-09-08 "Six measurements ended the guessing. MAC loopback (TxConfig 17|18) returns the frame -- descriptors, DMA addresses and both engines are correct. PHYstatus is 0x0093 = link up, 1000 Mbps, full duplex, so the port has a live gigabit partner. 199 broadcasts from en8 and 199 from en15, across five boots each, produced no receive event; the greeting sent to 10.10.10.1 AND to 255.255.255.255 both reported sent and neither arrived. Everything this kernel owns is proven correct, and the wire carries nothing either way: the K16's second port is plugged into something that is not this Mac. The DHCP requests bearing bus3's MAC are frames the FIRMWARE composed with that address; they say nothing about which port they left by."
   :superseded-measurement "which interface the second NIC is on: en8 vs en15 Ipkts across one boot. If the offers to ...b6:31 leave on en15 while that NIC listens on en8, the server must reply on the interface the request arrived on."
   :blocked "the loop STOPPED at ~09:33: with bus3's link up the firmware netboots from it, the server accepts only bus2's MAC (PXE_EXPECTED_MAC), and the board no longer reaches bus2 -- 0 boots / 0 TFTP in 150 s after ~1.2 boots/min. efc5d540 has never been served. Owner action needed (ports 67/69 cannot be rebound from here): move the second cable to en8, or restart the PXE server accepting both MACs, or disable network boot on NIC2 in firmware."}
  {:id "K3" :repo "aiueos" :hyps ["LAN2 carries debug AND control (owner, 2026-09-08)"]
   :state :working-on-hardware :k16-tip "(see branch)" :image "21f6a79d"
   :root-cause "(def k16-debug-ip 168493058) ; 0x0A0A0A02 -- and 0x0A0A0A02 is 168430082. 168493058 is 10.11.0.2; the peer constant was wrong the same way. The two baked IPv4 header checksums were computed from the addresses in the COMMENTS, so frame and checksum could never agree: every datagram was dropped by the Mac in ip_input and every ARP asked for 10.11.0.1."
   :found-by "emitting the built IPv4 header on the bus2 netlog as DD <byte> pairs -- 45 00 00 1D 00 00 00 00 40 11 66 C5 0A 0B 00 02 FF FF FF FF, stored checksum 0x66C5, computed 0x70C4. The Mac had been counting the drops all along: netstat -s -p ip bad header checksums, +2 per boot (the two greetings), 5 boots -> +10."
   :measured-after "bad header checksums +0 per boot; the greeting arrives every boot (from=10.10.10.2:9000 hex=42); arp -an resolves 10.10.10.2 at 70:70:fc:b:b6:31 on en8; P -> p (hex=70, netlog D9 40); R -> r (hex=72, netlog DB) and the board reboots. LAN2 carries debug output and control including reboot; bus2 carries PXE and the stream."
   :also-landed ["bus3 speaks ARP (request at init, reply to requests for 10.10.10.2)" "unsolicited frames broadcast, answers go to the requester's MAC" "MAC loopback self-test" "debug-tick: a missing BAR is 3, not 0" "the first cycle of every run reports the tick" "PXE server accepts a MAC list (the second NIC can netboot too)" "the sink binds the wildcard"]
   :lesson "Six correct measurements ruled out things that were never wrong -- cabling twice, the destination MAC, the IP shape, the rings, the DMA, the PHY -- because the drop is counted on neither end of a two-sided debugging session but in the RECEIVING KERNEL's statistics. `netstat -s` is the first place to look the moment `it says it sent and nothing arrived`."}]
 :meta
 ["12:2x: origin/main CLAUDE.md now carries an owner rule (2026-09-07): new operational tooling is kbb-first; nbb is forbidden as a NEW script host, existing nbb scripts are migration candidates (skill nbb-to-kbb-migration; ops missing from kbb are added on the kbb side, not by shrinking the guest). Written today in nbb before/while that rule landed, therefore migration candidates: os/aiueos/tools/k16-bridge.cljk, k16-bus3-sink.cljs, k16-rig-check.cljs, verify-wire-bytes.cljs, verify-store-tautology.cljs; root detectors verify-catch-swallow / verify-fuel-literal-vs-policy / verify-checker-floors / verify-status-sink / verify-grammar-authority-floor extend the nbb-only registry (E11 followed the brief and flagged the conflict). The bridge, sink and rig-check need UDP/TCP capabilities kbb does not have yet (lib/kbb: fs env browse proc str) -- that gap is the first kbb capability extension this loop must file, not a reason to keep writing nbb."
  "11:0x: /Users/junkawasaki/github/wt (the aiueos-n2-tranche1 worktree and two others) was removed from disk at ~10:48 by something outside this session; everything committed was already on origin/k16-stream-20260906 (b0f6c1e, 10:39). Ledger edits continue from a fresh worktree of that tip."
  "Six agents were cut off by one session limit; work in worktrees survived and was salvaged by hand. Salvage cost ~45 min and found one real defect in the salvage itself: cherry-pick -q | tail swallowed a failure (0 commits landed, push went through with nothing) -- the $? class again."
  "verify-checker-floors was written to the hypothesis, not to a measured red; the hypothesis was about empty-but-present inputs and the detector probes missing inputs. Same lesson as the kernel: a gate is landed when it has been seen red on the thing it names."]}
