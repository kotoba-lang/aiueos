;; Rank 01 — 2026-09-07 09:10 JST. Generate returned 61 hypotheses from 5 agents
;; (tcp 14, cr3 6, koto 15, silence 12, amu 14); two agents (lang, rig) hit a
;; session rate limit and are relaunched. Ranking is pairwise on: decisive test
;; exists · blast radius · cost · predicted gain · dependency depth. Hypotheses
;; without a decisive test are not ranked. Raw vectors live in the agents'
;; task outputs; ids are stable and cited in commits.
{:iteration 1
 :ranked
 [;; --- Evolve now (tier 1, landed in one artifact: kernel da7699c7) -------
  {:id "tcp-h1"  :elo 1720 :decision :landed :note "rearm + step head at establishment; predicted F5 -> F1/F7"}
  {:id "tcp-h3"  :elo 1690 :decision :landed :note "SYN_SENT poll 8000; 21 -> real SYN retransmit"}
  {:id "tcp-h4"  :elo 1680 :decision :landed :note "debug-run-cycles 6 so DE/reset is reachable at 2^20"}
  {:id "tcp-h14" :elo 1650 :decision :landed :note "tcb-init/held-table-store compare stores; build-ack unexported"}
  {:id "tcp-h7"  :elo 1620 :decision :landed :note "RST -> CLOSED (FA), SYN in ESTABLISHED dropped (FB)"}
  {:id "tcp-h10" :elo 1600 :decision :landed :note "IPv4+TCP checksum before admit (FC)"}
  {:id "tcp-h9"  :elo 1580 :decision :landed :note "advertised window = rcv-wnd - held"}
  ;; --- Evolve now (tier 2, delegated to isolated worktrees) -------------
  {:id "cr3-h1"  :elo 1760 :decision :delegated :agent "E1 kotoba-native" :note "context slots r9-relative; QEMU gate returns; near-proven by bytes"}
  {:id "cr3-h4"  :elo 1640 :decision :delegated :agent "E1" :note "abs32-into-text scan as a packaging refusal"}
  {:id "amu-h5"  :elo 1700 :decision :delegated :agent "E2 amu" :note "--fuel dropped on --source-path route"}
  {:id "amu-h6"  :elo 1610 :decision :delegated :agent "E2" :note "refuse --fuel vs policy disagreement"}
  {:id "amu-h10" :elo 1660 :decision :delegated :agent "E2" :note "project diagnostics mapped through source-map"}
  {:id "koto-h2" :elo 1680 :decision :delegated :agent "E4 kototama" :note "doctor/parity gates cannot fail"}
  {:id "koto-h3" :elo 1620 :decision :delegated :agent "E4" :note "three R2 statuses disagree"}
  {:id "koto-h7" :elo 1640 :decision :delegated :agent "E4" :note "trap agreement accepts tool failures"}
  {:id "koto-h9" :elo 1600 :decision :delegated :agent "E4" :note "lifecycle receipt literal :kernel-reboot? false"}
  {:id "silence-h9"  :elo 1660 :decision :delegated :agent "E5 root" :note "checker floors: exit 2 on zero evidence"}
  {:id "silence-h8"  :elo 1620 :decision :delegated :agent "E5" :note "fuel literal vs policy detector"}
  {:id "silence-h3"  :elo 1600 :decision :delegated :agent "E5" :note "catch-swallow detector"}
  {:id "silence-h12" :elo 1640 :decision :delegated :agent "E5" :note "register the family in orgs-detectors.edn"}
  ;; --- Evolve next round (depends on tier 1/2 readings) -----------------
  {:id "amu-h1"  :elo 1740 :decision :next :depends ["amu-h2" "amu-h11" "cr3-h1"] :note "loader-driven tick re-entry with replenish = the resident kernel"}
  {:id "amu-h14" :elo 1600 :decision :next :note "main/tick split in kernel.kotoba"}
  {:id "amu-h7"  :elo 1620 :decision :next :note "#UD ISR names fuel exhaustion"}
  {:id "amu-h8"  :elo 1560 :decision :next :note "read-only kernel-fuel-remaining (4-repo pin dance)"}
  {:id "amu-h11" :elo 1580 :decision :next :note "receipt derives fuel from ELF bytes + policy"}
  {:id "tcp-h5"  :elo 1590 :decision :next :note "window is empty by construction until we send; bridge greeting for the debug profile"}
  {:id "tcp-h6"  :elo 1540 :decision :next :note "split wrong-port from wrong-ack in receive-syn-ack"}
  {:id "tcp-h8"  :elo 1560 :decision :next :depends ["tcp-h5"] :note "out-of-order delivered as in-order; held table has no writer"}
  {:id "tcp-h11" :elo 1550 :decision :next :note "TX waits 2e8 vs 2^20 fuel"}
  {:id "koto-h1"  :elo 1640 :decision :next :note "tender fuel is a lifetime cap per Instance"}
  {:id "koto-h5" :elo 1560 :decision :next :depends ["koto-h6"]}
  {:id "koto-h6" :elo 1580 :decision :next :note "one deny! helper with reasons"}
  {:id "silence-h1" :elo 1560 :decision :next :note "(* 0 x) status sinks in drivers"}
  {:id "silence-h2" :elo 1580 :decision :next :depends ["silence-h1"]}
  {:id "silence-h10" :elo 1600 :decision :next :depends ["silence-h9"] :note "guest-grammar unreadable == forbids nothing"}
  ;; --- Reflected out ----------------------------------------------------
  {:id "amu-h3"  :decision :rejected-by-design :note "no guest-callable replenish (interrupt ABI names why)"}
  {:id "amu-h13" :decision :superseded-by "amu-h5"}
  {:id "tcp-h2"  :decision :withdrawn :note "same bytes as tcp-h1; timing already falsifies retransmit reading"}
  {:id "cr3-h3"  :decision :diagnostic-only}
  {:id "amu-h9"  :decision :constraint-on "amu-h1" :note "use replenish-bytes, never a hand imm32"}]
 :meta
 {:instruments-failed-this-iteration
  ["netlog receiver: bound, alive, not recording; local probes to 10.77.0.1 not delivered on any port/binary while 127.0.0.1 is -- unexplained; board frames may still arrive via en15 (untested until the board reboots)"
   "the board did not self-reset after the 220160-byte boot: with the RX ring live a cycle costs ~64k+ calls, 2^20 buys ~6-12 cycles, debug-run-cycles 256 was never reached, the run ended in a silent ud2 (tcp-h4)"
   "two Generate agents died on a session rate limit at ~23:00 JST; relaunched 09:10"]}}
