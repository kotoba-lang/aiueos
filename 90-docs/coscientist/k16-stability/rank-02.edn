;; Rank 02 — 2026-09-07 11:5x JST. After Evolve 01 landed (E1 E4 E5 E6 E7 G6 merged; E2 open
;; on 13 GitHub checks; K1 deployed, unmeasured). Ranking is pairwise on the same five axes as
;; rank-01: decisive test exists · blast radius · cost · predicted gain · dependency depth.
;; Every :measured below was read off a wire, a test run, or a log -- not predicted.
{:iteration 2
 :landed-since-rank-01
 [{:id "cr3-h1"  :where "kotoba-native#153 4ca092eb" :measured "abs32 stores gone; scan red on a re-hardcoded slot, 5 failures; 194/2815/0" :remaining "MPRCD on QEMU needs amu's kotoba-native pin >= e4b46e5 (sibling PR amu#853 is at d5fdf8cb)"}
  {:id "cr3-h4"  :where "kotoba-native#153" :measured "elf64.cljc refuses text reaching the fixed data offset (:text-reaches-kernel-data)"}
  {:id "amu-h5"  :where "amu#856 (open)" :measured "--fuel on the --source-path route: sealed 512 -> 4096"}
  {:id "amu-h6"  :where "amu#856 (open)" :measured "--fuel 8192 + policy 4096 refused :fuel-declared-twice on both routes"}
  {:id "amu-h10" :where "amu#856 (open)" :measured "project refusals name the authoring module + line"}
  {:id "koto-h2" :where "kototama#135 14bb92d7" :measured "doctor/parity compute their verdict; could-not-run is exit 2; real CLI table 120->121 -> exit 1"}
  {:id "koto-h3" :where "kototama#135" :measured "one R2 status; md cell edited -> FAIL quoting both"}
  {:id "koto-h7" :where "kototama#135" :measured "tool failure is :unmeasured, not :trap; pinned literals per runtime; reverse break -> 10 failures in exactly the 4 new tests"}
  {:id "koto-h9" :where "kototama#135" :measured "receipts derive from state; :kernel-reboot? true -> 2 failures"}
  {:id "silence-h3" :where "root#2978 a24492a9" :measured "catch-swallow: aiueos 184/18, kototama 107/28"}
  {:id "silence-h8" :where "root#2978" :measured "fuel-literal-vs-policy: aiueos 285/12"}
  {:id "silence-h9" :where "root#2978" :measured "checker-floors seen red: kototama check-q9 PASS with no count; the count regex was wrong twice first (2 false reds, 1 false green), all three in main's history"}
  {:id "silence-h12" :where "root#2978" :measured "3 registry entries, 70 detectors parse"}
  {:id "lang-h5"  :where "kotoba-sema#60 affe05db" :measured "both disagreeing uses named"}
  {:id "lang-h6"  :where "kotoba-sema#60" :measured "catch-all split; 21 arity sites name head + count"}
  {:id "lang-h12" :where "kotoba-sema#60" :measured "unbound symbol names nearest defined"}
  {:id "lang-h9"  :where "aiueos#292 (k16 branch)" :measured "wire-bytes.edn 39 entries / 4 channels; verifier 0 findings clean, red on a planted duplicate; ADR omitted FA FB FC"}
  {:id "lang-h13" :where "aiueos#292" :measured "verify-store-tautology: 129 sites (73 direct, 55 let-bound, 1 operand-echo); READ-BACK sites 0"}
  {:id "tcp-h1"   :where "kernel 47b8c209, boot 11:03" :measured "no F5 in five windows; F1 x45 -- prediction F5 -> F1/F7 held on the F1 side; F7 was unreachable because the bridge never spoke"}
  {:id "tcp-h4"   :where "kernel 47b8c209" :measured "FALSIFIED at 6: no DE, run died in ud2 after 5 A9 cycles + 1 A4 cycle; cadence 4 deployed in f039f387"}
  {:id "rig-g6"   :where "aiueos#293" :measured "k16-rig-check 9 instruments; found the unlinked bus3 sink; 10.77.0.1 local UDP delivered 14/14"}]
 :ranked
 [;; --- Evolve now ---------------------------------------------------------
  {:id "amu-h1"  :elo 1780 :decision :next-evolve :depends ["amu#853 or a follow-up pin to kotoba-native 4ca092eb"] :note "per-cycle fuel replenish via loader-driven tick re-entry -- every cadence number above is a workaround for its absence"}
  {:id "tcp-h15" :elo 1720 :decision :next-evolve :note "NEW (from the 47b8c209 reading): ISS and therefore the source port are per cycle, not per boot, so every boot reuses the same 4-tuples. The bridge's RST-close removes the Mac-side state, but the kernel should also vary ISS per boot (a boot counter in the RW page, TSC low bits, or the RTL8125 timer). Decisive test: two boots, disjoint port sets in bridge.log"}
  {:id "lang-h13b" :elo 1700 :decision :next-evolve :depends ["lang-h13"] :note "rewrite the 129 tautological store checks (73 direct + 55 let-bound) to READ-BACK or drop them; kernel-rebuilding; the detector must read 0 afterwards and the kernel must still boot"}
  {:id "lang-h9b" :elo 1660 :decision :next-evolve :depends ["lang-h9"] :note "the DA channel puts raw MAC bytes on the sentinel wire (70 70 FC 0B B6 31 read as an FC checksum verdict once). Prefix DA payload bytes or move them to a length-tagged form; register the change"}
  {:id "tcp-h5b" :elo 1640 :decision :measuring :note "bridge greeting is deployed (k16-bridge.cljs); prediction for f039f387: F7 F8 + data ACK, then FA/0x70 on the RST, DE after 4 cycles"}
  {:id "rig-h2"  :elo 1620 :decision :next-evolve :note "the bus3 sink sat on an unlinked inode for hours; k16-rig-check now catches it, but the sink itself should refuse to run when its target path is gone (reopen by name per receipt, or a launchd job that restarts on the check's exit 1)"}
  {:id "build-h1" :elo 1600 :decision :landed :note "boot build re-says the kernel verifier's OK line or refuses (was /dev/null) -- landed in 964da2e..dc9573b"}
  ;; --- Next round --------------------------------------------------------
  {:id "amu-h14" :elo 1600 :decision :next :note "main/tick split in kernel.kotoba (prerequisite of amu-h1)"}
  {:id "amu-h7"  :elo 1620 :decision :next :note "#UD ISR names fuel exhaustion -- would have made the 47b8c209 death visible on the wire instead of inferred from a missing DE"}
  {:id "amu-h8"  :elo 1560 :decision :next :note "read-only kernel-fuel-remaining; lets the cadence be a fuel check instead of a constant"}
  {:id "tcp-h6"  :elo 1540 :decision :next :note "split wrong-port from wrong-ack in receive-syn-ack -- would have named cycle 6's A4 cause on the wire"}
  {:id "tcp-h8"  :elo 1560 :decision :next :depends ["tcp-h5b"]}
  {:id "tcp-h11" :elo 1550 :decision :next}
  {:id "koto-h1" :elo 1640 :decision :next}
  {:id "koto-h5" :elo 1560 :decision :next :depends ["koto-h6"]}
  {:id "koto-h6" :elo 1580 :decision :next}
  {:id "silence-h1" :elo 1560 :decision :next}
  {:id "silence-h2" :elo 1580 :decision :next :depends ["silence-h1"]}
  {:id "silence-h10" :elo 1600 :decision :next :depends ["silence-h9"]}
  {:id "lang-h1"  :elo 1640 :decision :next}
  {:id "lang-h4"  :elo 1560 :decision :next}
  {:id "lang-h8"  :elo 1520 :decision :next}
  {:id "lang-h10" :elo 1540 :decision :next}
  {:id "lang-h11" :elo 1500 :decision :next :depends ["amu-h10"]}
  {:id "cr3-h2"  :elo 1500 :decision :next :note "kernel-probe-nx-execute and the non-returning #PF handler still assume the data page at 0x110000 (E1's open question) -- same drift class as cr3-h1"}]
 :meta
 {:coordinator-errors-this-iteration
  ["landed kototama#135 by running the merge and the branch deletion in one command; the merge had failed (draft PR) and GitHub auto-closed the PR when the branch vanished -- restored from refs/pull/135/head"
   "pushed an unreadable evolve-01.edn (dc9573b): the reader check ran, printed its error, and the commit went ahead because nothing gated on its exit -- fixed in two commits; commits now `|| exit 1` on the reader"
   "the verify-checker-floors count regex was wrong twice before it was right (2 false reds, then 1 false green) -- both recorded in root main's history rather than squashed"]
  :instruments
  ["/Users/junkawasaki/github/wt was removed from disk at ~10:48 by something outside this session; committed work was on origin; worktrees now live under com-junkawasaki/.claude/worktrees (owner instruction 11:1x)"
   "the resource-guard build lock was held by a sibling session (agent-amu-pins) for minutes at a time; E4 waited, E2 waited, E6 ran one 5 s single-namespace JVM directly"
   "amu main is protected with 13 GitHub checks; the PR is the only landing path there (CLAUDE.md 2026-09-06 correction confirmed)"]}}
