#:kotoba.cli{:ok? false, :code :kbb/no-jvm-free-backend, :message "the native backend does not host [:fs/browse]. The JVM-free js backend does host this surface: kbb --backend js (or drop --backend for auto)", :data {:capabilities [:fs/browse], :requested-backend :native, :native-hosted [:fs/app-data], :js-hosted [:env/read :fs/app-data :fs/browse :git/run :proc/exec]}}
