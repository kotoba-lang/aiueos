(defn main []
  (let [state (cap-call 2 0)
        sent (cap-call 3 42)
        written (cap-call 4 42)
        receipt (cap-call 5 0)]
    (if (and (> (bit-and state sent) 0)
             (> written 0)
             (= receipt 42))
      42 0)))
