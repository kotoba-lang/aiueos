(defn aiueos-syscall-range-valid [pointer length lower upper]
  (and (> upper lower)
       (> length 0)
       (>= pointer lower)
       (< pointer upper)
       (<= length (- upper pointer))))

(defn main [] 0)
