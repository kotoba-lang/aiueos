;; Two virtio-gpu 2D resources (ADR-0094). Count is Kotoba; C mechanism only.
{:format :aiueos.gpu-two/v1
 :native {:target :x86_64-aiueos-kernel-v1
          :export nil
          :count-from "kotoba_aiueos_wm_hit"}
 :vectors {:two-resources-flush-two :ok
           :one-resource :leftover
           :one-scanout :leftover}
 :trusts-caller-for [:native-phase6-compositor
                     :physical-boot
                     :multi-scanout]
 :verification {:off-target :impossible
                :reason :kir-refuses-kernel-memory
                :evidence "clojure -M:compositor guest-gpu-two"}
}
