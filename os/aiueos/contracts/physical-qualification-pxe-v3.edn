;; v3 follows the physical v2 code 250 observation.  The first framebuffer
;; marker returned under the owned CR3, then dereferencing the loader-owned
;; boot-info pointer stopped before the GOP contract completed.  Retain the
;; scalar handoff and bounded UEFI memory map in kernel-owned low memory before
;; installing the final page-table root.
{:format :aiueos.physical-qualification-pxe/v3
 :extends "physical-qualification-pxe-v2.edn"
 :observed-input {:artifact-sha256
                  "ea08f1375500e152ea04722c10aeb477661cfaed7e79c1ac416bdde97d5ded54"
                  :physical-result-code 250
                  :meaning :loader-owned-boot-info-unmapped-after-cr3}
 :owned-handoff {:boot-info :kernel-bss
                 :memory-map :kernel-bss
                 :memory-map-maximum-bytes 131072
                 :retained-before :owned-cr3}
 :safety {:internal-disk-writes? false
          :usb-runtime-writes? false
          :ssd-install? false}}
