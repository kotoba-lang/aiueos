{:format :aiueos.value-handle-plan/v1
 :source "kotoba/value-handle-plan.kotoba"
 :capacity 4096
 :non-reuse true
 :operations {0 :allocate 1 :resolve 2 :cid-of 3 :release}
 :result-layout {:action [0 7]
                 :handle [8 20]
                 :next-handle [21 33]
                 :live-count [34 46]
                 :reject 0}
 ;; The symbol was already required, just not declared. `verify-value-handle-plan`
 ;; has always compared the compiled object's export against this exact string;
 ;; the contract stayed silent, so amu's export allowlist had nothing to
 ;; transcribe and -- once amu#626 made that table refuse an unlisted name
 ;; instead of handing out `kotoba_aiueos_probe` -- the object could not be
 ;; packaged at all. A requirement that lives only in the checker is a
 ;; requirement no producer can read.
 ;;
 ;; No `:required-opcode` here, unlike the arena: this planner holds no lock.
 ;; It is a pure decision over its five arguments.
 :native {:target :x86_64-aiueos-kernel-v1
          :export "kotoba_aiueos_value_handle_plan"
          :imports []}
 :vectors
 [{:name :allocate-first :args [0 1 0 0 0] :expected 17184063745}
  {:name :resolve-live :args [1 2 1 1 1] :expected 17184063746}
  {:name :cid-of-live :args [2 2 1 1 1] :expected 17184063747}
  {:name :release-live :args [3 2 1 1 1] :expected 4194564}
  {:name :released-handle-rejected :args [1 2 1 1 0] :expected 0}
  {:name :forged-handle-rejected :args [1 2 1 2 1] :expected 0}
  {:name :live-capacity-rejected :args [0 1 4096 0 0] :expected 0}
  {:name :released-slot-not-reused :args [0 4097 0 0 0] :expected 0}
  {:name :allocate-last-monotonic-handle
   :args [0 4096 4095 0 0]
   :expected 70377337257985}]}

